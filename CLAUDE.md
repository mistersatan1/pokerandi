# 포랜디 (포켓몬 랜덤 디펜스) — Claude Code 작업 안내

브라우저에서 `index.html` 을 열면 바로 돌아가는 1인용 랜덤 디펜스. HTML/CSS/바닐라 JS, 빌드 도구·게임 엔진 없음.
ES 모듈을 안 쓰는 이유: 더블클릭(file://)으로 열어도 CORS 없이 돌아가게. **`index.html` 의 `<script>` 순서가 곧 의존성이다.**

작업을 시작하기 전에 **`VERSION.md` 맨 위 몇 개 절**을 읽는다 — 무엇을 왜 바꿨고 측정값이 어땠는지가 최신순으로 있다.

---

## 명령

```bash
npm run check        # 검사 5종 — 커밋 전에 반드시 전부 통과
npm run autoplay     # 자동 플레이 20판 (노멀)
npm run tierpower    # 등급·얻는 법·역할별 강함 표
npm run build        # 테스터용 단일 HTML → dist/포켓몬랜덤디펜스_테스트.html
npm run screenshot   # 빌드 후 실제 크로미움으로 장면 캡처 → dist/*.png
```

캡처 준비(한 번): `npm install && npx playwright install chromium`.
장면은 `tools/screenshot.js` 안에서 게임 함수(`RPD.Game.startRun`, `RPD.GameManager.setWave`, `RPD.FieldManager.select` …)로
상태를 만든 뒤 찍는다. 새 기능은 그 기능이 보이는 장면을 추가해서 찍는다.

## 구조 — 자주 고치는 곳

| 파일 | 내용 |
|---|---|
| `js/core/RPD.js` | 설정값(Config) · 등급(Tiers) · 모드(Modes: 노멀·챌린지 70R, 보스 러시 20R, 엔드리스) |
| `js/data/pokemon.js` | 152종(1세대 151 + 왕구리) + 초월 폼 2. `summon:true` 소환 가능 · `hidden:true` 주문 전용 |
| `js/data/recipes.js` | 조합식 101개(경로 103 — 근육몬·성원숭은 OR-조합식). 수정법은 `docs/RECIPES_GUIDE.md` |
| `js/data/spells.js` | 채팅 주문 — 히든 31 · 불멸 5 · 초월 2 |
| `js/data/roletuning.js` | 역할 보정(광역·연쇄 ↓, 단일·보스킬러 ↑, 버퍼 오라 ↑) — **craftpower.js 보다 먼저 로드** |
| `js/data/craftpower.js` | 까다로운 조합 보너스 · 히든 1.5배 · 불멸 1.6배 · 초월 1.8배(재료 합 기준, 역할 보정 포함) |
| `js/data/auras.js` | 버퍼 버프 7축(공속·치명·치피·사거리·쿨다운·방어무시·보스피해) |
| `js/data/waves.js` | 적 체력 곡선 — ~50 ×1.109, 51~60 ×1.07, **61R 계단 ×3.5**, 이후 ×1.02 |
| `js/systems/*Manager.js` | 규칙. 골드 상점 `GoldShopManager` · 정예 `EliteManager` · 보상 `RewardManager` |
| `js/ui/UIManager.js` | 화면 대부분. 크다 — 파일 상단 구조 설명부터 읽는다 |

## 작업 규칙 (사용자와 합의된 것)

1. **측정 먼저.** 밸런스는 감이 아니라 `tools/autoplay.js` 28~40판 표본으로. 20판은 노이즈가 커서 결론을 내지 않는다.
   조정 전·후를 같은 조건으로 재서 표로 보고한다.
2. **버그가 기능보다 먼저.**
3. **핵심 설계 의도를 지킨다.** 제안이 핵심 메커닉과 부딪히면(예: 소환을 없애자) 메커닉을 지우지 말고 주변(경제 등)을 조정한다.
   부딪히는 점은 분명히 말한다.
4. **결정엔 이유를 붙인다.** VERSION.md 기록도 "무엇을 · 왜 · 측정값" 형식.
5. **솔직하게.** 잘못 단정했거나 실수했으면 그렇게 말한다. 수치가 목표를 못 맞췄으면 숨기지 않는다.
6. **요청이 여러 개면 하나씩** — 하나 적용 → 결과 보고 → 확인 받고 다음.
7. **적용할 때마다 실제 플레이 캡처(PNG)를 보여 준다.** 새로 그린 목업이 아니라 테스트판을 실제로 띄워 찍은 것.
8. **요청 밖의 변경**(예: 목표를 맞추려고 다른 수치를 건드림)은 따로 짚어서 알린다.
9. 끝나면 `npm run check` 전부 통과, `VERSION.md` 맨 위에 새 절 추가.

## 이 코드베이스에서 겪은 함정

- **새로 만들기 전에 이미 있는지 찾는다** (`grep -rn`). 중단된 작업이 비슷한 기능을 반쯤 남겨 둔 적이 여러 번 있다
  (조합 보정을 두 번 만들어 보너스가 두 번 곱해질 뻔했다 · 후반 곡선 · 봇 실험 옵션 중복).
- **게임 코드(js/)는 `process.env` 를 읽지 않는다.** 실험용 덮어쓰기는 `tools/autoplay.js` 에서만(검사가 막는다).
- **HTML 문자열을 만들 때 속성을 `class="…"` 따옴표 안에 넣지 않는다** — 브라우저가 속성으로 안 읽어 클릭이 통째로 죽는다.
  검사는 "글자가 있는가"가 아니라 속성을 파싱해서 본다(bootsmoke 의 `realAttr`).
- **`|| 1` 로 기본값을 주면 0 이 1 이 된다** (정예 lifeCost 0 이 1 로 깎인 버그). 0 이 의미 있는 값이면 `== null` 로.
- 백그라운드 측정: 도구 호출이 끝나면 자식 프로세스가 죽는 환경이 있다 — `setsid nohup … &` 로 띄우고 결과 파일을 기다린다.
  `pkill -f autoplay` 는 자기 셸까지 죽일 수 있다(`pkill -x node`).
- `tools/gen1/*` 는 옛 규칙용 생성기라 실행하면 멈추게 막혀 있다(돌리면 조합식이 덮어써진다). `tools/selftest.js` 는 v1 시절 것.
  `CHECKPOINT.md` 는 오래된 기록 — 현재 상태는 VERSION.md.

## 측정 방법 (`tools/autoplay.js`)

`node tools/autoplay.js <판수> <모드> [난이도]` — 모드: `NORMAL` `BOSS_RUSH` `ENDLESS` `CHALLENGE`.

| 환경변수 | 뜻 |
|---|---|
| `ELITE_SAFETY=1` | 정예를 "보드를 보고 판단해서" 부르는 봇 — **밸런스 측정 기본값** |
| `IMMORTAL=1` | 불멸 재료를 모으고 직접 외치는 봇 |
| `GIVE_IMMORTAL=2` | 50R 에 가장 약한 전설 3마리씩을 치르고 불멸 2마리로 바꾼다 — "불멸을 갖춘 판" 비교용 |
| `WALL_STEP` · `LATE` | 61R 벽 높이 · 51~60 성장률 실험 |
| `NO_SHOP` · `NO_ELITE` · `NO_CRAFTPOWER` | 골드 상점 · 정예 · 조합 보정을 끄고 비교 |
| `SPECIES_LOG=파일` | 종별 "필드에 있던 1초당 실제 피해"를 JSON 으로 — 포켓몬 밸런스 점검용 |

요약의 "60R 도달 → 벽 넘김(65+)"은 60라운드 시점 불멸·초월 보유 여부로 나눈 것.
61 에서 죽어도 "61 도달"이라, 65+ 도달과 클리어율로 판단한다.

## 현재 목표치 (최근 측정, VERSION.md 참고)

- 노멀 70R: 불멸 없이 60 도달 → 벽 넘김 약 54% · 클리어 약 23% / 불멸 2마리 → 벽 넘김 96% · 클리어 약 44%.
  **"불멸·초월급이 없으면 60을 넘기 힘들게"** 가 사용자 요구 — 다른 걸 고쳐 이게 깨지면 벽(`wallStep`)으로 되돌린다.
- 보스 러시 약 60~70% · 중앙 20.
- 역할별 실전 초당 피해(등급 중앙 = 1): 연쇄·광역이 여전히 1.4~2.2 로 가장 높다. 버퍼는 피해 지표로 안 잡힌다.

## 열려 있는 문제 (사용자 결정 대기 포함)

- 봇이 불멸을 스스로 거의 못 만든다 — 사람이 60R 전에 불멸을 갖출 수 있는지는 **플레이로 확인 필요**.
- 61R 인데 HUD 라운드 옆에 "2R 뒤 희귀함"이 뜬다 — 다음 해금 표시 버그로 보임(미확인).
- 메타몽: 인수인계에는 "소환 가능"이었으나 조합식 v2 표대로 히든으로 넣었다 — 결정 대기.
- 시라소몬(희귀함·히든)이 흔함 3마리분으로 지나치게 싸다(괴력몬 전설도 연쇄로 싸다).
- 조합 병목(소환 불가 재료가 3곳 이상에 쓰임) 17곳.
- 엔드리스가 길다(중앙 77R · 약 23분).
