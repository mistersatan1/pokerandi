# v2-redesign (진행 중)

## 세션 35 — 포켓몬 정보 카드에 공격속도 칸

**무엇을:** 정보 카드 능력치에 `공격속도` 칸을 따로 뒀다(예: `1.67/초`, 모든 버프·보정이 들어간 실효값).
원래는 `공격` 칸 숫자 옆에 이름 없이 `×1.67` 로만 붙어 있었다.
칸에 마우스를 올리면 기본값과 버프·보정으로 달라진 비율이 나온다(예: `기본 1.00회/초 · 버프·보정 +67%`).
칸이 7개가 돼서 `사거리 강화` 는 한 줄을 통째로 쓴다(`.is-wide`).

**왜:** 사용자 요청. `×1.67` 이 공격속도인지 알 수 없었다.
기본값 대비 비율도 칸에 넣어 봤는데, 카드 폭 250px 에서 세 칸짜리 줄에는 `1.67 +67%` 가 잘려서 툴팁으로 뺐다.

**캡처 스크립트 버그 수정:** `tools/screenshot.js` ② 장면이 `field:select` 를 `slot` 없이 한 번 더 보내서 정보 카드가 바로 닫혔다.
그걸 빼고, 카드 확대 장면 `03_unitcard.png` 를 추가했다.

## 세션 34 — 골드 상점: 아래에 칸이 더 있으면 끝을 흐린다

**무엇을:** 골드 상점 본문(`.gshop__body`)에 아직 내릴 칸이 남아 있으면 `is-more` 클래스를 붙인다.
이 클래스는 본문 아래 36px를 마스크로 흐리게 하고, 끝까지 내리면 떨어진다.
`GoldShopUI.updateFade` 가 창을 그릴 때 · 스크롤할 때 · 창 크기가 바뀔 때 다시 판단한다.

**왜:** 1440×900에서 타입 마지막 줄(드래곤 · 강철 · 페어리)이 잘려 보였다. 버그는 아니고 본문이 스크롤되는 구조였다.
그런데 스크롤바가 6px에 배경색과 거의 같아서, 아래에 칸이 더 있다는 걸 알아채기 어려웠다.

**측정 (실제 크로미움, 본문 보이는 높이 / 내용 높이 → 흐림):**

| 화면 | 높이 | 흐림 |
|---|---|---|
| 1920×1080 | 552 / 552 | 없음 |
| 1440×900 | 503 / 552 | 있음 → 끝까지 내리면 없음 |
| 1280×720 | 388 / 552 | 있음 |

캡처: `npm run screenshot` 에 `01b_goldshop_bottom.png`(끝까지 내린 장면)을 추가했다.

## 세션 33 — 역할 보정 · 버퍼 다양화

### 1. 역할 보정 (`js/data/roletuning.js`)

| 역할 | 피해 | 공격속도 |
|---|---|---|
| 광역 AOE_DPS | ×0.85 | — |
| 연쇄 CHAIN_DPS | ×0.8 | — |
| 단일 SINGLE_DPS | ×1.2 | ×1.15 |
| 보스킬러 BOSS_KILLER | ×1.35 | ×1.2 |

피해 배율은 스킬에도 실린다(스킬이 실효 공격력을 쓴다).

### 2. 버퍼 — 버프 종류 3 → 7, 세기 상향 (`js/data/auras.js`)

새 축: **사거리 · 스킬 쿨다운 · 방어 무시 · 보스 피해**(기존 공격속도 · 치명타율 · 치명타 피해).
9종 전부 서로 다른 조합이고, 주변 공격력 오라는 역할 보정으로 ×1.4.
방어 무시 · 보스 피해는 `EnemyManager.damage` 가 `source.auraExtras` 로 읽어 기본 공격 · 스킬 · 특성 모두에 실린다.
유닛 카드 "받는 버프" 줄에 새 축도 나온다.

| 버퍼 | 버프 |
|---|---|
| 삐삐 | 사거리 +10% |
| 푸린 | 스킬 쿨다운 -12% |
| 고라파덕 | 방어 무시 +15% |
| 골덕 | 치명타율 +10% · 방어 무시 +20% |
| 럭키 | 공격속도 +15% · 쿨다운 -10% |
| 마임맨 | 치명타 피해 +40% · 사거리 +10% |
| 푸크린 | 공격속도 +12% · 치명타 피해 +40% |
| 픽시 | 보스 피해 +30% · 치명타율 +12% · 사거리 +10% |
| 뮤 | 공격속도 +15% · 쿨다운 -15% · 보스 피해 +25% |

상한: 공속 40% · 치명 30% · 치피 100% · 사거리 25% · 쿨다운 30% · 방어 무시 50% · 보스 피해 60%.

### 측정 (노멀 80판 · 역할별 실전 초당 피해, 등급 중앙 = 1 · 특별함/희귀함/전설)

| 역할 | 전 | 후 |
|---|---|---|
| 연쇄 | 4.43 / 3.03 / 2.87 | **2.10 / 2.23 / 1.39** |
| 광역 | 2.12 / 1.50 / 2.52 | 2.08 / 1.00 / 1.64 |
| 단일 | 1.05 / 0.95 / 1.05 | 1.08 / 1.08 / 0.99 |
| 보스킬러 | 0.75 / 1.36 / **0.30** | 0.98 / 1.83 / **0.66** |

버퍼의 몫은 피해 지표로 안 잡힌다(전설 버퍼 0.27 은 버프를 주는 역할이라 당연하다).

### 부작용과 되돌림

- 버퍼·단일·보스킬러 상향이 연쇄·광역 하향보다 커서 노멀 클리어 5% → 32%, 불멸 없이 60을 넘는 비율이
  약 17% → 약 40% — **"불멸 없이는 60을 넘기 힘들게"가 무너졌다.**
- 게다가 불멸 재료(전설 단일·보스킬러 ↑)와 불멸 결과(썬더 연쇄·파이어 광역 ↓)가 반대로 보정돼
  불멸 2마리를 갖춘 판(19%)이 안 갖춘 판(27%)보다 못했다. → 불멸·초월 배율을 **역할 보정까지 포함한
  재료 합** 기준으로 다시 계산(`roletuning.js` 를 `craftpower.js` 앞에 싣는다).
- 61라운드 벽 ×2.5 → **×3.5**.

| 벽 ×3.5 (40판 + 불멸 40판) | 60 도달 후 벽 넘김(65+) | 클리어 |
|---|---|---|
| 불멸 없음 | 54% | 약 23% |
| 불멸 2마리 | **96%** | 약 44% |

검사: redesigncheck 13개(역할 보정 4 · 버퍼 새 축 4 · 실제 피해 반영 2 · 조합 전부 다름 · 7축 사용 · 전 버퍼 보유) — 210 통과.

## 세션 33 — 포켓몬 전체 밸런스 점검 (측정만, 조정 안 함)

`SPECIES_LOG=파일 node tools/autoplay.js N` — 종별 "필드에 있던 1초당 실제 피해"(스킬·특성 포함)를 모은다.
누적 피해는 조합 재료로 쓰이면 사라지고 필드 시간도 제각각이라 0.5초마다 차이만 센다. 노멀 80판(불멸 추구 봇 20판 포함).

**역할별 실전 초당 피해 (같은 등급 중앙 = 1)**

| 역할 | 특별함 | 희귀함 | 전설 |
|---|---|---|---|
| 연쇄 CHAIN_DPS | 4.43 | 3.03 | 2.87 |
| 광역 AOE_DPS | 2.12 | 1.50 | 2.52 |
| 제어 CONTROL | 4.69 | 2.00 | 1.84 |
| 단일 SINGLE_DPS | 1.05 | 0.95 | 1.05 |
| 감속 SLOW | 1.00 | 0.92 | 0.93 |
| 보스킬러 BOSS_KILLER | 0.75 | 1.36 | **0.30** |
| 버퍼 BUFFER | 1.54 | 0.55 | 0.48 |

- **연쇄·광역이 모든 등급에서 2~4배.** 기본 공격 수치로는 광역이 단일의 1.1~1.6배인데 실전은 2~3배 —
  한 라운드에 13~26마리가 몰려오는 구조라 여러 마리를 때리는 쪽이 몫을 다 가져간다. 특별함 코일(연쇄 · 소환 가능)이
  특별함 필드 시간의 27%를 차지한다.
- **전설 보스킬러가 0.30.** 괴력몬 ×0.27 인데 전설 필드 시간 14%(가장 싸게 만들어지는 전설이라 봇이 자주 든다).
  보스킬러는 보스전 몫이라 전체 초당 피해가 과소평가되는 역할이지만, 전설 보스가 70라운드 중 7번뿐이라 실제로도 손해다.
- 버퍼는 팀 버프가 몫이라 피해 지표로는 과소평가된다(해석 주의).
- **따로 약한 일반 개체**(수치·실전 모두): 거북왕(전설 감속, 기본 공격 0.51), 고지(희귀함 ×0.37), 킹크랩(희귀함 보스킬러 0.61),
  독파리(희귀함 ×0.59), 꼬렛(안흔함 ×0.42), 레트라(특별함 ×0.57).
- 히든은 봇이 주문을 거의 안 써서 필드 시간이 0~6% — 실전 표본 부족. 수치상으로는 설계대로 1.5배.
- 거의 안 쓰인 일반 조합: 딱구리 · 갸라도스(전설, 비싼 경로) — 만들어지지 않는 전설.

## 세션 33 — 70라운드 후반 곡선: "불멸·초월 없이는 60을 넘기 힘들게"

### 곡선 (`js/data/waves.js` growthTo)

| 구간 | 라운드당 체력 | 의도 |
|---|---|---|
| ~50 | ×1.109 (그대로) | |
| 51~60 | **×1.07** | 완만 — 전설을 모으고 불멸·초월을 준비할 구간 |
| 61 | **×2.5 계단** | 벽 — 불멸·초월 없이는 여기서 막힌다 |
| 62~70 | **×1.02** | 거의 평평 — 벽을 넘은 보드는 끝까지 간다 |

먼저 들어가 있던 안(60 이후 라운드마다 ×1.17 기울기)은 61~62 에선 오히려 예전보다 쉬워서 불멸 없는 봇이
22/23 으로 60을 넘었다. 벽은 기울기가 아니라 계단이어야 했다. 벽 뒤를 ×1.07 로 두었을 땐 불멸이 있어도
65~69 에서 다 죽었다(클리어 0~4%) — 벽 뒤에 또 벽을 만들지 않게 ×1.02 로 눕혔다.

### 불멸·초월 = 재료 합의 1.6배 · 1.8배 (`js/data/craftpower.js` 3번 규칙)

불멸이 "벽을 넘는 열쇠"가 되려면 확실한 전력 도약이어야 하는데, 재료(전설 3마리) 합 대비 들쭉날쭉했다:
썬더 2.47 · 뮤츠 1.67 · 파이어 1.45 · 뮤 1.37 · **프리져 0.80** · **초월 뮤츠 1.00** · 초월 리자몽 3.14
(앞서 들어가 있던 일괄 ×2 를 포함한 값). 프리져·초월 뮤츠는 만들면 손해였다.
"까다롭게 만들수록 세다" 원칙대로 종마다 재료 합에 맞춰 불멸 1.6배 · 초월 1.8배(조각까지 쓰니까)로 통일했다.

### 측정 (노멀 · 판단형 정예 봇 · 30~40판씩)

| 벽 | 불멸 없음 — 60 도달 후 클리어 | 불멸 2마리(전설 6마리 치르고) — 클리어 |
|---|---|---|
| 없음 | 47% | — |
| **×2.5 (채택)** | **약 17%** (두 번 잰 평균) | **45%** |
| ×3 | 14% | 27% |
| ×4 | 14% | 0% |

×2.5 에서 불멸 유무의 차이가 가장 크다(2.6배). 불멸 없는 판의 약 83% 가 60~69 에서 끝난다.
"벽 넘김" 지표는 61 에서 죽어도 넘은 것으로 세던 것을 65+ 도달로 고쳤다(라이프 버퍼 때문에 여전히 노이즈가 있어 클리어율을 기준으로 삼았다).

**봇은 불멸을 스스로 거의 못 만든다** — 불멸 추구 봇(`IMMORTAL=1`)도 60라운드까지 10~14판 중 1판.
그래서 "불멸 있음"은 `GIVE_IMMORTAL=2`(50라운드에 가장 약한 전설 3마리씩을 치르고 불멸로 바꾼다)로 쟀다.
사람이 불멸을 얼마나 현실적으로 만드는지는 플레이 테스트로 봐야 한다.

### 정리

- 앞선 중단 작업이 남긴 중복을 걷어냈다: 게임 코드가 테스트용 환경변수를 읽던 곳(waves.js · craftpower.js) 제거,
  봇의 같은 목적 옵션 두 벌(`GIVE_TOP` / `GIVE_IMMORTAL`)과 60라운드 지표 두 벌을 하나로.
- 검사: redesigncheck 12개(곡선 모양 4 · 환경변수 없음 · 불멸·초월 7종 배율) — 197 통과.
- **실제 화면 캡처**: `node tools/screenshot.js` — 테스터 빌드(dist)를 크로미움으로 띄워 골드 상점 ·
  61라운드 장면을 찍는다. 크로미움은 `/opt/pw-browsers` 에 있다(`~/.cache` 가 아니다).

## 세션 33 — 노멀·챌린지 50 → 70라운드

RPD.Modes.NORMAL·CHALLENGE 의 finalWave 50 → 70. 보스 러시(20)·엔드리스(무한)는 그대로.

체력·물량 곡선(js/data/waves.js)은 라운드 50에 맞춰 손으로 멈춘 값이 없다 — 지수 성장(hpGrowth)과
"심화" 구간(41라운드 이후 물량 상한 26마리)이 원래 엔드리스용으로 끝없이 이어지게 짜여 있어서,
코드 변경은 finalWave 숫자 하나면 됐다. 대신 50라운드에 고정돼 있던 두 가지를 70에 맞게 옮겼다.

- **보스 스킨**: "오메가몬(최종형)"이 50라운드가 아니라 **70라운드**에 뜨게 구간 경계를 옮겼다.
  안 옮기면 진짜 마지막 보스 10라운드 전(50R)에 최종형이 나오고, 정작 마지막(70R)엔 그냥
  엔드리스용 순환 스킨(오메가몬 군체 등)이 뜬다.
- **보스 보상**: 6·7번째 보스(60·70R) 항목을 추가했다. 7번째가 이제 진짜 마지막이라
  5번째(전설 1)보다 확실히 크게(전설 2 + 소환권 5) 줬다 — 안 그러면 마지막 보스가
  중간 보스보다 시시해진다.

검사: redesigncheck 1개 교체(70라운드 확인), uicheck 2개 추가(최종형 스킨이 70에 있다 ·
6·7번째 보상이 5번째보다 크다).

### ⚠ 측정 — 20라운드를 늘리자 노멀 클리어율이 78% → 18%로 떨어졌다

40판씩, 판단형 정예 봇 기준.

| 모드 | 50라운드(직전 측정) | 70라운드 |
|---|---|---|
| 노멀 | 클리어 78% · 중앙 50 | 클리어 **18%** · 중앙 60 |
| 챌린지 | 클리어 50% · 중앙 50 | 클리어 **3%** · 중앙 50 |

**원인**: hpGrowth(1.109)는 지수라서 20라운드를 더 살면 적 체력이 그만큼 더 곱해진다 —
70라운드 체력은 50라운드 체력의 약 **7.9배**(1.109²⁰). 보드 성장(추가 20라운드 동안 버는 골드·
합성·전설)이 이 배율을 못 따라가면서 격차가 벌어졌다. 지수 성장 곡선을 그대로 20라운드
늘리면 뒤로 갈수록 가팔라지는 건 이 곡선의 성질상 자연스러운 결과다.

**결정 필요**: hpGrowth 를 낮춰(또는 후반 구간만 완만하게) 70라운드에서도 예전과 비슷한
클리어율(45~50%)로 되돌릴지, 아니면 "70라운드는 원래 더 어려운 진짜 엔드게임"으로
지금 난이도를 그대로 둘지. 손 안 대고 다음 지시를 기다린다.

## 세션 33 — 조합 난이도 보정 (까다로운 조합 보너스 · 히든 1.5배)

`js/data/craftpower.js` · 점검 `node tools/tierpower.js` · 끄고 재기 `NO_CRAFTPOWER=1 node tools/autoplay.js`

**진단(보정 전)**: 강함은 조합식 v2 이전 데이터를 등급 비율로만 옮긴 값이라, 같은 등급 안에서
흔함 환산과 강함의 상관이 특별함 -0.08 · 희귀함 -0.07 · **전설 -0.50(비쌀수록 약했다)**.
히든은 재료를 더 먹는데 일반 조합의 0.79~0.97배였다.

**규칙**
1. 까다로운 조합 보너스 — 조합 전용 종(소환으로 안 나오고 히든 아님)이 같은 등급 조합 평균보다 재료가 많이 들수록
   공격력 +. `0.6 × (환산 ÷ 평균 − 1)`, 0 ~ +35%. 평균 이하는 깎지 않는다.
2. 히든 1.5배 — 같은 등급 일반 조합(보너스 포함) 평균의 1.5배. 종 하나하나가 아니라 히든 무리 평균 기준이라
   역할 차이(광역·단일·지원)는 남는다.
3. 그 전에, 같은 등급·같은 역할 안에서 옛 데이터의 기본 편차를 가운데로 75% 모은다(`PULL 0.75`) —
   이게 없으면 편차가 보너스를 덮어 "비싸게 만든 게 더 약한" 역전이 남는다.
   0.6 에선 역전 3곳, **0.75 에서 2곳**(고지↔버터플, 윈디↔리자몽 — 재료 차이가 작고 상대 쪽 특성이 DPS 를 올리는 차이), 0.85 도 2곳이라 0.75.

흔함·불멸·초월·소환 가능 종은 대상이 아니다. 선택한 칸 정보에 "까다로운 조합 +N%" / "히든 보정 ×N" 표시.

**경위**: 이 모듈은 앞선(사용 한도로 중단된) 작업에서 만들어져 연결까지 됐지만 기록·검사·측정이 없었다.
이번에 같은 기능을 `power.js` 로 한 번 더 만들었다가 발견하고 지웠다(둘 다 연결되면 보너스가 두 번 곱해진다).
redesigncheck 에 10개(히든 1.5배 등급별 · 소환 종 제외 · 대상 외 · 상한 · 역할 내 역전 20% 미만 · 실효 공격력 반영).

### 전체 밸런스 (40판씩 · 판단형 정예 봇)

| 모드 | 보정 없음 | 보정 있음 | 과거 목표 |
|---|---|---|---|
| 노멀 | 60% · 중앙 50 | **78%** · 중앙 50 | 30~50% |
| 보스 러시 | 60% · 중앙 20 | **75%** · 중앙 20 | ~60% |
| 챌린지 | 28% · 중앙 49 | **50%** · 중앙 50 | ~20% |

**보정 없이도 노멀이 이미 60% 로 쉬워져 있었다** — 정예(④)·보스 보상(⑤)·사거리 강화로 쌓인 몫이다.
조합 보정이 여기에 +15~22%p 를 더 얹었다. 노멀 최종 보드: 특별함 7.3 · 희귀함 9.7 · 전설 3.0.
적 체력으로 되돌릴지는 결정 대기.

참고(앞선 중단 작업의 측정, 보정 전 · 노멀 20판): 쉬움 100% · 보통 70% · 어려움 10% · 지옥 0%.

## 세션 33 — 강화를 "사거리 강화"로

골드 상점(판 전체 공격력 · 재료로 써도 유지)이 생긴 뒤 개체 강화(공격력 +12%)는 골드당 이득에서 늘 져서
판당 1.6~3.6회밖에 안 쓰였다. 상점이 절대 주지 않는 **사거리**로 역할을 바꿨다.

| | 전 | 후 |
|---|---|---|
| 효과 | 공격력 +12%/회 (최대 +60%) | **사거리 +8%/회 (최대 +40%)** |
| 전체 사거리 포켓몬 | 강화 가능 | **막음**(늘릴 게 없다 — 골드 낭비 방지). 버튼에 "전체 사거리" |
| 표시 | 강화 +N | 선택한 칸 정보에 "사거리 +N% · 경로 330→410px", 버튼 툴팁에도 같은 미리보기 |
| 비용 · 최대 레벨 | — | 그대로 |

이 게임의 핵심인 "칸마다 덮는 경로가 다르다"와 직결된다 — 구석 자리·근접용 칸에 앉은 좋은 포켓몬을 살리는 도구.
공격력은 이제 골드 상점(G)의 몫이다.

### 발견: "강화 레벨 승계"가 구현돼 있지 않았다

5차 경제 재설계 기록에 "조합 시 재료 강화 레벨의 절반을 결과물에 승계"라고 적혀 있지만, 코드에는
투자 골드(방출 환급용)만 이어지고 **레벨은 한 번도 이어진 적이 없었다**(세션 31 원본도 같다).
조합 재료로 쓸 포켓몬을 강화하면 그대로 날아갔다 — 강화가 늘 외면받은 원인 중 하나였다.
이제 조합식·주문 모두 **재료 중 가장 높은 강화 레벨의 절반**을 결과물이 이어받는다.

### 측정 (판단형 정예 봇)

| 모드 | 판당 강화 (전 → 후) | 판당 골드 상점 | 클리어 |
|---|---|---|---|
| 노멀 (40판) | 1.6 → **8.3** | 16.0 | 48% (전 45%) |
| 보스 러시 (40판) | 3.6 → **12.0** | 21.3 | 68% (전 70%) |
| 챌린지 (20판) | 2.2 → **5.4** | 9.1 | 20% (전 35% · 20판 노이즈 범위) |

난이도는 거의 그대로, 강화와 상점이 각자 쓰인다. 봇은 "이 칸에서 덮는 경로가 늘어나는 비율 × 그 포켓몬 DPS"로
강화 가치를 매겨 골드 상점과 비교한다.

검사: redesigncheck 6개(사거리 +8% · 공격력 불변 · 커버리지 증가 · 미리보기 = 실제 · 최대 +40% · 전체 사거리 차단 ·
조합 승계), uicheck 1개 교체(스킬이 실효 공격력을 따른다 — 공격력을 올리는 수단을 골드 상점으로).

## 세션 33 — 요청 5/5: 보스 보상 상향 (+ 요청 ②로 무너진 보스 러시)

보스를 놓쳐도 라이프 5 만 깎여 "안 잡아도 그만"이었다. 잡으면 확실히 남게 올렸다.

| | 전 | 후 |
|---|---|---|
| 처치 골드 | 150 + 5×R (10R 200 · 40R 350) | **300 + 25×R** (10R 550 · 40R 1,300) |
| 1번째 보스 | 특별함 1 | **특별함 2 + 소환권 2** |
| 2번째 | 소환권 3 + 특별함 1 | **희귀함 1 + 특별함 1 + 소환권 3** |
| 3번째 | 희귀함 1 | **희귀함 2 + 소환권 3** |
| 4번째 | 소환권 3 + 희귀함 1 + 초월의 조각 | **전설 1 + 소환권 3 + 초월의 조각** |
| 5번째 | 전설 1 | **전설 1 + 희귀함 1** |
| 이후(엔드리스) | 소환권 3 + 희귀함 1 | 희귀함 2 + 소환권 3 |

- HUD "다음 보스 보상" 예고와 처치 팝업에 **골드가 맨 앞에** 나온다(목표가 보여야 버틸 이유가 생긴다).
- **엔드리스 버그 수정**: 보상을 "라운드 번호"(10·20…)로 찾아서, 7라운드마다 보스가 오는 엔드리스는
  70라운드 전까지 보상을 한 번도 못 받았다. 이제 "몇 번째 보스"로 찾는다. 보스 러시는 여전히 10·20R 만.
- 보스를 놓친 벌칙(라이프 5)은 그대로 — 요청은 보상 쪽이었다.

### 보스 러시 — 닭과 달걀

요청 ②(호위 제거) 뒤 중앙 5라운드에 무너졌던 것을 여기서 풀었다. 추적해 보니 골드 문제가 아니었다 —
1라운드부터 보스뿐이라 시작 보드(DPS 221)로 첫 보스(체력 4,459)를 못 잡고, **못 잡으면 보스 골드도 0**.
보스 골드를 아무리 올려도 소용없었다. 비교:

| 안 | 클리어 | 도달 중앙 |
|---|---|---|
| 보스 골드만 상향 | 10% | 5 |
| + 초반 보스 체력 ×0.6 | 20% | 5 |
| **+ 시작 골드 ×4 (1,200G)** | **70%** | **20** |

호위가 주던 초반 자금을 시작 골드로 미리 준다(`startGoldMul 1.5 → 4`). 매 라운드 보스라 `bossGoldMul 0.5`.

### 최종 측정 (20판씩 · 판단형 정예 봇 `ELITE_SAFETY=1`)

| 모드 | 클리어 | 도달 중앙 | 판당 골드 상점 / 강화 | 정예 성공률 |
|---|---|---|---|---|
| 노멀 | 45% | 50 | 16.8 / 1.6 | 94% |
| 보스 러시 | 50% | 20 | 18.1 / 3.6 | 78% |
| 엔드리스 | — | **77** (최소 63) | 49.3 / 4.2 | 100% |
| 챌린지 | 35% | 48 | 16.5 / 2.2 | 95% |

- 골드 수급(정예 ④ + 보스 ⑤)이 생기자 **골드 상점이 판당 7회 → 17회**로 제 역할을 한다.
  가격은 지금 그대로 둔다.
- 개체 [강화]는 여전히 판당 1.6회 — 상점(판 전체 · 재료로 써도 유지)이 골드당 이득에서 늘 이긴다.
  **강화는 사실상 상점에 흡수됐다.** 없앨지·역할을 바꿀지는 결정이 필요하다.
- **엔드리스가 크게 늘었다** (중앙 42 → 77, 판 시간 22.7분). 보상을 처음으로 제대로 받게 된 데다
  7라운드마다 보스 골드가 들어온다. 판이 길어진 게 의도와 맞는지 확인 필요.

## 세션 33 — 요청 4/5: 정예 소환 (E)

HUD [정예 소환] 버튼 · 단축키 **E**. `js/systems/EliteManager.js` + `js/ui/EliteUI.js`, 적 정의 `elite_1~3`.

| 등급 | 체력(이번 라운드 잡몹 전체 대비) | 참가비(1R 기준 · 라운드마다 +5%) | 처치 보상 | 놓치면 |
|---|---|---|---|---|
| 하급 | ×2.0 | 30G | 참가비 ×2.5 + 안흔함 1마리 | 라이프 -20% + 5라운드 소환 금지 |
| 중급 | ×4.5 | 70G | 참가비 ×3 + 특별함 1마리 | 라이프 -30% + 5라운드 소환 금지 |
| 상급 | ×9.0 | 160G | 참가비 ×3.5 + 희귀함 1마리 | 라이프 -40% + 5라운드 소환 금지 |

- 한 번에 한 마리, **한 라운드에 한 번**. 라이프 벌칙은 시작 라이프 기준 비율(모드·난이도 무관하게 같은 무게).
- 소환 금지 = 일반 소환(소환권 포함) + 정예 소환 모두. 소환 버튼에 "금지 NR" 표시. 라운드 무료 지급은 유지.
- **정예는 라운드 진행을 막지 않는다** — `EnemyManager.waveAliveCount()` 에서 빠진다. 보스 라운드도 보스만 기다린다.
- 포켓몬 보상은 히든 제외(보스 보상과 같은 풀). 필드·창고가 꽉 차 있으면 골드를 절반 더 준다.
- 필드에서 보라 이중 고리로 표시, 결과는 배너로.

### 첫 패스는 버렸다 — 위험이 없는 금광이었다

처음 수치(제한 없음 · 체력 ×0.8/1.6/3 · 보상 ×4)로 재자, **조심스러운 봇도 99% 잡고 판당 3.9만 골드**를 벌어
클리어율이 5% → 80% 로 튀었다. 보드는 단일 대상에 강하다(보스 ×1.9~2.9 도 늘 잡힌다) — ×3 은 위협이 아니었다.

### 지금 수치 — 판단력에 따라 갈린다 (노멀 20판씩)

| 봇 성향 | 정예/판 | 성공률 | 정예 골드/판 | 클리어 | 도달 중앙 |
|---|---|---|---|---|---|
| 정예 안 씀 | — | — | — | 0~5% | 44~45 |
| 신중(예상 피해 ≥ 체력×3 일 때만) | 2.0 | 100% | 257G | 0% | 45 |
| 판단형(예상 피해 ≥ 체력×1) | 8.4 | 96% | 2,606G | **30%** | 46 |
| 도박꾼(살 수 있는 최고 등급) | 2.9 | **30%** | 451G | 0% | **28** |

보드를 읽고 부르면 크게 벌고, 무작정 상급을 부르면 금지·라이프 벌칙으로 판이 일찍 무너진다.
봇 성향은 `ELITE_SAFETY=<배수>`, 끄려면 `NO_ELITE=1`.

**0 이 "없음"으로 바뀌던 버그 두 개도 잡았다** — 정예(lifeCost 0)가 새면 `lifeCost || 1` 때문에 라이프가 1 더 깎였고,
처치 보상(bounty 0)에 최소 1골드가 붙었다.

검사: redesigncheck 17개(참가비 · 체력 · 중복 불가 · 라운드당 1회 · 라운드 진행 안 막음 · 처치 보상 골드·포켓몬 ·
놓침 라이프·금지 5라운드·해제 · 보스 라운드 · 새 판 초기화 · 골드 부족), bootsmoke 2개(창 · 금지 표시).

## 세션 33 — 버그: 필드 조합식 재료가 전부 안 눌림

**원인(내 실수)**: 요청 1 수정 때 "안 밝혀진 히든 재료는 못 누르게" 하려고 `data-def`/`data-go` 속성을
조건부로 붙였는데, 네 곳 모두 **`class="…"` 따옴표 안에** 넣었다. 브라우저는 그걸 class 이름의 일부로 읽어
속성이 아예 없는 것으로 친다 — 그래서 히든뿐 아니라 **모든 재료가 클릭되지 않았다**.
(필드 조합식 재료 칸 · 히든 줄 재료 칸 · 조합식 창 재료 · 히든 결과의 조합식 창 재료)

**수정**: class 를 먼저 닫고 속성을 뒤에 붙였다. 이제 규칙은 의도대로다 —
일반 재료(이상해풀 등)는 눌러서 하위 조합식으로 내려가고, 안 밝혀진 히든(피카츄 등)만 안 눌린다. 밝히면 눌린다.

**검사가 못 잡은 이유와 보강**: 기존 검사는 `data-def="…"` 라는 **글자가 HTML 어딘가에 있는지**만 봤다 —
class 안에 들어가 있어도 글자는 있으니 통과했다. bootsmoke 에 4개 추가:
class 값을 걷어 낸 뒤 **진짜 속성으로 남는지** 파싱해서 본다(이상해풀 눌림 · 피카츄 안 눌림 · 조합식 창 안에서
다시 하위로 내려가기 · 밝힌 뒤 눌림), 그리고 조합식·사전·보유·골드 상점 전 패널에서 class 값 안에 `=` 가 섞인 곳이
없는지. 버그를 일부러 되살려 새 검사가 실패하는 것까지 확인했다.

## 세션 33 — 요청 3/5: 골드 상점 (G)

HUD [골드 상점] 버튼 · 단축키 **G** · Esc 로 닫기. `js/systems/GoldShopManager.js` + `js/ui/GoldShopUI.js`.

한 판 동안 유지되는 **판 전체 강화**다. 개체 하나를 올리는 [강화]와 달리, 올린 레벨이
앞으로 뽑거나 조합할 개체에도 붙는다 — 조합 재료로 갈아 넣어도 사라지지 않는다.

| 칸 | 효과 | 가격(1레벨) | 비고 |
|---|---|---|---|
| 등급 7칸 — 흔함·안흔함·특별함·희귀함·전설·🔒히든·불멸+초월 | 공격력 +6%/레벨 | 40 / 60 / 90 / 130 / 180 / 110 / 250 | 히든은 강함 등급 대신 **히든 칸만** 받는다 |
| 타입 — 1세대에 있는 타입만(강철·악 제외) | 공격력 +5%/레벨 | 60 | 두 타입이면 **높은 쪽 하나만** |

- 최대 10레벨, 레벨마다 가격 ×1.4. 공격력 배율 = (1+타입) × (1+등급), 개체 강화·시너지·스킬 버프와 곱.
- 카드마다 현재/다음 효과, 지금 필드에서 혜택받는 마리 수, 가격이 보인다.
- 검사: redesigncheck 14개(가격·레벨·곱연산·이중 타입·히든 칸·불멸초월 공유·최대 레벨·조합 후 유지·새 판 초기화),
  bootsmoke 1개(창 렌더 · 클릭 구매 · 갱신).
- 봇: 남는 골드로 "필드 DPS 를 골드당 가장 많이 올리는" 칸을 산다(개체 강화와 골드당 이득 비교).
  `NO_SHOP=1 node tools/autoplay.js 20` 으로 상점 없는 기준치를 잴 수 있다.

### 측정 (노멀 20판)

| | 상점 없음 | 상점 있음 |
|---|---|---|
| 클리어 | 0% | 0% |
| 도달 중앙 / 최대 | 44 / 47 | 44 / 50 |
| 판당 골드 상점 | — | 7.8회 |
| 판당 개체 강화 | 0.1회 | 0.1회 |
| 평균 보유 골드 | 48 | 45 |

- 상점은 쓰이지만 **판도는 거의 안 바뀐다** — v2 는 골드 자체가 빠듯해 봇이 소환에 거의 다 쓰고,
  상점은 남는 잔돈만 흡수한다. 골드 수급(요청 ④ 정예 소환, ⑤ 보스 보상)이 늘어야 비로소 의미가 생긴다.
  **가격은 ④·⑤ 이후 다시 잰다.**
- 개체 강화는 상점 이전부터 이미 판당 0.1회 — 상점이 뺏은 게 아니다. v2 경제에서 강화가 다시 죽어 있다
  (5차 경제 재설계 때 18.4회까지 살렸던 것). 같이 ④·⑤ 이후 확인할 항목.

## 세션 33 — 요청 2/5: 보스 라운드엔 보스만, 사라질 때까지 다음 라운드 안 옴

- `WaveData.build()` — 보스 라운드 호위(6+wave/4마리) 스폰을 없앴다. 이제 `entries.length === 1`(보스 하나뿐).
- `WaveManager` CLEARING — 보스 라운드는 `carryOverLimit`(5.5초)을 무시한다. 보스가 필드에서
  완전히 사라질 때까지(잡히거나 끝까지 걸어 나가거나) 다음 라운드가 겹쳐 들어오지 않는다.
  그 외 라운드는 원래대로 carryOverLimit 이 지나면 다음 라운드가 겹친다.
- 검사(`redesigncheck`): 보스 라운드 entries 1개 확인, carryOverLimit 넘겨도 안 넘어가는지,
  잡으면 넘어가는지, 끝까지 걸어 나가도(라이프만 깎이고) 넘어가는지 4개 추가.

### ⚠ 측정 결과 — 보스 러시 모드가 심각하게 무너졌다

호위를 없앤 대가를 20판씩 재 봤다. **보스 러시(매 웨이브가 보스)가 거의 못 하는 수준으로 무너졌다** —
호위가 그 모드의 사실상 유일한 골드 수입원이었기 때문이다(3차 밸런스 기록에 이미 경고돼 있던 지점).

| 모드 | 전(직전 측정) | 후 |
|---|---|---|
| 노멀 (보스 5회/50R) | 클리어 0% · 중앙 42 | 클리어 0% · 중앙 45 (거의 그대로) |
| 엔드리스 (보스 7웨이브마다) | — | 클리어 0% · 중앙 42 (거의 그대로) |
| 챌린지 (보스 5회/50R) | — | 클리어 0% · 중앙 40 (거의 그대로) |
| **보스 러시 (매 웨이브 보스)** | 클리어 50~75% · 중앙 20 | **클리어 15% · 중앙 5** — **붕괴** |

보스 러시는 5~9라운드에서 20판 중 13판이 죽는다. 최종 보드가 흔함 1.6·안흔함 5.2·특별함 4.3마리뿐 —
골드가 없어 보드를 못 키운다. 노멀·엔드리스·챌린지는 보스가 드물어 영향이 거의 없다.

**손 안 댐 — 요청 5(보스 보상 상향)와 겹치는 문제라 그쪽에서 같이 다룰지, 지금 바로
보스 러시만 별도로 만질지 사용자 판단이 필요하다.**

## 세션 33 — 요청 1/5 수정: 필드 조합식은 "발견한 히든"만

지난 기록(주문 공개)을 사용자 피드백으로 다시 손봤다. 핵심 차이: **조합 사전은 미발견도 다 보여 주지만,
필드 조합식(하단 패널)은 발견 전엔 아예 줄이 없다.**

| 항목 | 이번에 정한 것 |
|---|---|
| 미발견 히든 | 필드 조합식에 **줄 자체가 없음**(조합 사전에서만 확인) |
| 발견한 히든 | 필드 조합식에 뜨고, **줄을 누르면 채팅 없이 바로 조합**(일반 조합식과 같은 손맛) |
| 불멸·초월 | 필드 조합식에 **절대 안 뜸** — 조합 사전 전용 |
| 등급 칩 | **[히든]** 칩 신설. 히든은 안흔함/희귀함 등으로 안 쪼개고 전부 이 칩 하나에 모은다.
  다른 등급 칩(안흔함~전설)에는 히든이 안 섞인다 |
| 재료로 쓰인 미발견 히든 | 정상 조합식 안에서도(예: 샤미드=이브이🔒+쥬쥬) **그 재료만 그림자+❔**로 가려진다. 발견하면 그 자리에서 바로 드러난다 |

구현: `spellViews()`가 `kind==='hidden' && knowsSpell` 인 것만 반환(불멸·초월·미발견 제외).
`matSecret(id)` 헬퍼로 "히든인데 아직 그 주문을 성공한 적 없는 재료"를 판정해 필드 쪽 재료 렌더링
4곳(조합식 줄·조합식 창·보유 상세·히든 줄 자체의 재료) 전부에 일관 적용.
등급 칩에 `HIDDEN` 가상 카테고리 추가, 히든 줄 클릭은 `SpellManager.cast(phrase)` 직접 호출(채팅 프리필 방식 폐기).

**버그 하나 잡음**: `renderRecipes()`의 재렌더 캐시(`recipeSig`)가 "재료 보유 수"만 보고 있어서,
재료 수가 그대로인 채 **발견 여부만 바뀌면** 다시 안 그려서 방금 발견한 게 계속 가려져 보였다.
서명에 재료별 발견 상태(`matSecret` 결과)를 넣어 고쳤다.

검사: bootsmoke 5개(발견 전 없음 · 발견 시 즉시 조합 · 등급 칩 분리 · 불멸·초월 배제 · 재료 마스킹 왕복).

## 세션 33 — 요청 1/5: 주문 재료·문구 공개, 결과는 그림자

아직 만들어 본 적 없는 주문도 **재료와 주문 문구를 전부 보여 준다.** 가리는 것은 결과뿐이다 —
그림은 그림자(실루엣), 이름은 ???. 한 번 성공하면 그림·이름이 드러난다.

| 어디 | 전 | 후 |
|---|---|---|
| 조합 사전(R) | 모르는 주문은 `???` + "재료 N마리"만 | 재료 · 문구 · 흔함 환산 전부, 결과만 그림자 |
| 필드 조합식 목록 | 주문이 아예 없었다 | **주문 줄 38개** 추가 — 재료 보유 수, 문구, 결과 그림자 |
| 조합식 창(재료 그림 클릭) | 모르면 "🔒 히든 — 재료 N마리" | 재료 보유 수 + 문구 |

- 주문 줄을 누르면 채팅 입력줄이 열리고 **문구가 미리 적힌다.** Enter 는 직접 누른다(외치는 손맛은 남긴다).
  재료가 모자라면 줄이 흔들리기만 한다.
- 모르는 결과의 그림자는 눌러도 조합식 창이 안 열린다 — 열리면 이름이 샌다.
- 조합 버튼·완성 배지는 예전처럼 조합식만 센다(주문은 채팅으로만).
- 결과 이름 검색은 발견한 뒤에만 된다. 주문 문구·재료 이름으로는 언제나 찾는다.
- 일반 조합식의 결과는 원래대로 다 보인다(v1 때 "목적지가 ??? 면 계획을 못 세운다"로 공개한 것).

검사: bootsmoke 에 3개(목록 주문 줄 · 발견 후 공개 · 사전 실제 렌더), uicheck 1개 교체.

## 조합식 개편 v2 (세션 32)

설계: `조합식_개편안_v2.md` + 병목 수정안 3건(격투계열 · 에스퍼비행 · 성원숭 및 나머지).
설계 문서의 표를 파싱해 기계적으로 옮겼다 — 손으로 옮기다 틀리는 일을 막으려고.

### 무엇이 바뀌었나

| | 전 (세션 31) | 후 |
|---|---|---|
| 소환 풀 | 판마다 78계열 중 27~30계열 추첨 | **152종 항상 전부** (PoolManager 삭제) |
| 재료 | `any:타입:등급` 와일드카드 76곳 | **전부 특정 포켓몬** |
| 조합 규칙 | 진화 = 진화 전 ×2 (+ 아무거나) | 진화보다 **연관성** (잉어킹 → 갸라도스처럼 건너뛰기 허용) |
| 조합식 | 91개 | **101개 · 경로 103** (근육몬·성원숭 OR-조합식) |
| 히든 | 등급(TH) 2종 | **얻는 법**(`hidden:true`) 31종, 안흔함~전설 |
| 불멸 | 별도 `_immortal` 폼 3종 | **원본 종 5종** — 파이어·썬더·프리져·뮤츠·뮤 |
| 주문 | 7개 | 38개 (히든 31 · 불멸 5 · 초월 2) |
| 전설 | 10종 | 24종 |
| 등급 구성 | — | 흔함 15 · 안흔함 48 · 특별함 33 · 희귀함 27 · 전설 24 · 불멸 5 |

초월은 v2 범위 밖이라 예전 그대로 둔다. 다만 v2 에서 불멸 뮤츠의 주문이 「나는 누구인가」라
**초월 뮤츠의 주문을 「나는 나다」로 바꿨다**(원래 초월 대사에서 따옴).

### 등급이 바뀐 83종

능력치는 기존 데이터의 등급별 계단값을 그대로 따랐다(공격력은 등급 기준값 비율, 광역 반경
40→58→78→98→118→130, 감속·방어깎기·연쇄·오라도 같은 방식). 개체별 편차는 유지했다.

- 새로 희귀함이 된 18종 + 특별함 이하에서 바로 전설이 된 3종(나인테일·파르셀·아쿠스타)에 **고유 스킬 21종** 신설
- 희귀함 → 전설로 올라간 15종 + 뮤의 스킬 16개를 **전설 예산으로 상향**(쿨다운 19~23초, 위력 ×1.5).
  스킬은 종마다 하나씩이라 제자리에서 고쳤다
- 새 전설 18종에 **전설 패시브** 신설. 전설이 24종이면 축(10개)이 모자라 한 축씩으로는 서로 다를 수 없다 —
  **두 축을 절반 세기로** 묶어 조합이 전부 다르게 했다. 서로 다른 패시브는 전부 겹치므로 약하게 잡았다
- 새로 특별함이 된 14종에 **특성** 신설(특별함은 전부 특성을 가진다는 규칙)
- 21종 스킬 연출 추가

### 시스템

- `RecipeData.list` 한 줄 = 경로 하나. `key` 는 `machoke` / `machoke#2`.
  `RecipeManager.craft(결과)` 는 되는 경로를 순서대로 찾고, `craft('machoke#2')` 는 그 경로만 쓴다
- 소환은 `summon:true` 종만, 보스 보상은 그 등급에서 히든을 뺀 전체, 조각 상점은 히든 판매 안 함
- 조합 사전: 🔒 히든 칩, 경로 표시. **아직 발견 안 한 주문의 히든 재료는 흔함 환산에서도 풀지 않는다**
  (풀면 숨긴 주문 재료가 사전에서 샌다). 숫자는 끝까지 센다
- 필드: 히든은 칸 왼쪽 위에 **H**. 테두리는 강함 등급 색 그대로
- 옛 생성기 `tools/gen1/*` 는 실행하면 멈추게 막았다(돌리면 v2 가 옛 규칙으로 덮어써진다)
- 자동 플레이 봇이 주문을 쓴다. 히든 31종이 전설 계보의 재료라 안 쓰면 측정이 틀어진다

### 검사

| 도구 | 결과 |
|---|---|
| `integritycheck` | 문제 없음 |
| `bootsmoke` | 이상 없음 |
| `rendersmoke` | 오류 없음 |
| `redesigncheck` | **통과 131** · 실패 0 (데이터 무결성을 v2 규칙으로 새로 씀 + OR-조합식·히든 12개) |
| `uicheck` | **통과 139** · 실패 0 (계열 추첨·TH·옛 불멸 폼 검사를 v2 규칙으로 1:1 교체) |

옛 규칙 중 v2 와 정면으로 부딪히는 것은 지웠다: "안흔함은 같은 흔함 2마리", "진화는 진화 전 ×2",
"같은 종 두 번은 진화 전뿐", "재료 3개 이하", "결과 하나당 조합식 하나".

### 밸런스 — 측정만 했다, 아직 조정 안 함

같은 봇 20판 (노멀 · 보통)

| | 세션 31 | v2 |
|---|---|---|
| 클리어 | 25% | **0%** |
| 도달 중앙 | 45 | 42 |
| 최종 보드 전설 | 0.7 | 0.3 |
| 판당 조합 / 주문 | 44 / — | 61 / 3.9 |

**v2 가 확실히 어렵다.** 와일드카드가 사라져 "같은 타입 아무거나"로 메우던 칸을 이제 특정
포켓몬으로 채워야 하고, 전설 계보에 히든(주문)이 끼어 전설 도달이 줄었다. 조정 방향은 다음 세션에 정한다.

### 설계에서 넘어온 알려진 문제

- **시라소몬(희귀함·히든) = 근육몬 + 알통몬** → 흔함 환산 3 (희귀함 평균 8.7 의 ×0.34).
  그 영향으로 괴력몬(전설)도 11 (×0.52). 격투 수정안에서 온 값
- 병목(소환 불가 재료가 3곳 이상에 쓰임)은 아직 17곳. 수정안의 대체재가 새 병목을 만든 곳이 있다
  (니드리노 4 · 왕콘치·이상해풀·딱충이·고지·독침붕·레어코일 3)
- 메타몽: 인수인계 요약에는 "소환 가능한 안흔함으로 남김"이라 돼 있지만 v2 표와 수정안은 히든이다.
  **표를 따랐다** — 결정 필요
- 수정안 파일의 하위 히든 주문 문구 6개가 v2 표와 달랐다(「튀어오르기 연습」 등). 마스터 표 문구를 썼다

---

## 2단계 — 히든 · 불멸 · 초월과 채팅 주문

조합식 목록에 없는 세 등급을 넣었다. 재료를 필드·창고에 모아 두고 **Enter**(또는 💬)로 입력줄을 열어
**주문을 외치면** 만들어진다.

| 등급 | 조건 | 횟수 | 연출 |
|---|---|---|---|
| 히든 | 정해진 재료 | 제한 없음 | 대사 + 히든 곡 |
| 불멸 | 재료에 전설 3마리 이상 | 제한 없음 | 대사 + 불멸 곡 + 은빛 오라 |
| 초월 | 전설 1마리 이상 + **초월의 조각** | **판당 1번** | 대사 + 초월 곡 + 금빛 오라 |

강함: 희귀함 < 히든 < 전설 < 불멸 < 초월 (평균 공격력 기준, 검사가 지킨다).

### 예시로 넣은 주문 7개

| 등급 | 결과 | 재료 | 주문 |
|---|---|---|---|
| 히든 | 왕구리 | 발챙이 + 슈륙챙이 + 메타몽 | 개구리의 왕 |
| 히든 | 뮤 | 메타몽 + 폴리곤 + 뮤츠 | 모든 포켓몬의 조상 |
| 불멸 | 불멸 파이어 | 파이어 + 썬더 + 프리져 | 세 개의 날개 |
| 불멸 | 불멸 망나뇽 | 망나뇽 + 리자몽 + 거북왕 | 용의 맹세 |
| 불멸 | 불멸 이상해꽃 | 이상해꽃 + 니드퀸 + 니드킹 | 대지의 왕국 |
| 초월 | 초월 뮤츠 | 뮤츠 + 뮤 + 초월의 조각 | 나는 누구인가 |
| 초월 | 초월 리자몽 | 리자몽 + 윈디 + 날쌩마 + 초월의 조각 | 꺼지지 않는 불꽃 |

주문 · 재료 · 대사는 `js/data/spells.js` 에서 고치고 더한다(맨 위에 방법).

### 규칙

- 띄어쓰기·문장부호·대소문자는 무시한다(`개구리 의 왕!` = `개구리의왕`).
- **틀린 주문은 아무 일도 없다** — 그냥 한 말로 채팅줄에 남는다. 무엇이 있는지 짐작할 수 없게.
- 주문은 맞는데 재료가 모자라면 `…아직 때가 아니다.` 한 줄. 초월은 `…초월의 조각이 필요하다.` ·
  `…초월은 한 판에 한 번뿐이다.`
- 초월의 조각: **40라운드 보스 보상**에 추가(일반·챌린지·엔드리스). 가지고 있으면 소환 버튼 위에 표시.
- 연출 동안 게임을 잠깐 멈춘다(대사를 읽는 사이 뚫리지 않게). 음악은 멈추지 않는다. 클릭하면 넘어간다.
- 처음 성공한 주문은 저장된다 → **조합 사전**의 히든·불멸·초월 칩에 주문과 재료가 공개된다.
  발견 전에는 `??? · 재료 N마리 · 조건` 으로 존재만 보인다.
- 특수 등급은 소환·조각 상점·계열 추첨·보스 보상 어디에도 나오지 않는다.

### 그림

불멸·초월은 새 그림 없이 원래 전설 그림을 쓰고(`spriteOf`), 필드에서 발밑에 숨쉬는 오라를 그린다
(불멸 은빛 · 초월 금빛, 칸 테두리도 등급색). 연출 창에서는 크게 빛나며 등장한다.

### 추가된 것

- 등급 3개(`TH` 히든 · `T6` 불멸 · `T7` 초월) — 기존 다섯 등급 순서(`TIER_ORDER`)는 그대로 두고
  `ALL_TIERS` · `tierRank` · `tierPower` 로 강함 순서를 따로 다룬다.
- 개체 7종(왕구리 · 뮤 · 불멸 3 · 초월 2), 스킬 7종, 불멸·초월 패시브 5종, 공격 연출 7종.
- 배경음 3곡(히든 · 불멸 · 초월), 효과음 1종.
- `js/data/spells.js` · `js/systems/SpellManager.js` · `js/ui/SpellUI.js`.

봇은 주문을 모르므로 난이도에는 영향이 없다(노멀 30판 두 번 27% · 47% — 직전 40% · 57% 와 같은 흔들림 범위).

`uicheck` 총 139 (+10).

## 조합 사전 (원랜디 조합식 사이트 참고)

ordsearch(원랜디 조합식 검색기)를 참고해 게임 안에 **조합 사전**을 만들었다. 상단 **조합 사전** 버튼 또는 **R**.

참고한 것
| 사이트 | 여기서 |
|---|---|
| 등급별 조합식 목록, "결과 · 재료 / 재료 / 재료" 한 줄 | 등급 칩(전체·안흔함~전설) + 한 줄에 결과·재료 그림 |
| **흔함 유닛 갯수** — 그 유닛에 흔함이 무엇이 몇 개 드는지 | **흔함 환산** — 조합식을 흔함까지 끝까지 풀어 "이상해씨 4 · 풀 희귀함 1", 합계 "약 13마리" |
| 이름·별칭 검색, 태그(단일·스턴…) | 이름 · **초성(ㄹㅈㅁ → 리자몽)** · 영어 id · 재료 이름 검색, 역할·타입 선택 |
| 조합 도우미 | 보유 기준 — 줄마다 보유 칸 수(`2/3`), "보유 기준 가까운 순" 정렬, 완성 가능하면 금색 테두리 |

더한 것
- 재료 이름 검색은 **흔함 환산까지 풀어서** 찾는다 — "캐터피"로 단데기뿐 아니라 버터플도 나온다.
- 재료를 누르면 그 포켓몬으로 다시 찾는다(계보를 따라 내려가기).
- "이번 판 계열만" 체크, 이번 판에 안 나오는 결과·재료는 흐리게.
- 스킬·특성 이름을 함께 보여 준다.
- 검색칸에 입력 중에도 Esc 로 닫힌다.

흔함 환산에서 "아무거나" 칸은 풀 수 없어 그 칸 그대로 센다(합계는 그 등급의 평균 흔함 수로 어림).

`js/ui/RecipeBook.js` · `uicheck` +3.

## 1세대 150종 로스터 (1단계: 데이터 · 조합식 · 계열 추첨 · 메타몽)

### 무엇이 바뀌었나

| 항목 | 전 | 후 |
|---|---|---|
| 로스터 | 55종 | **150종** (흔함 44 · 안흔함 35 · 특별함 38 · 희귀함 23 · 전설 10) |
| 조합식 | 39개 | **91개** |
| 시너지 | 11종 | **17종** (에스퍼·고스트·드래곤 복원 · 바위·얼음·강철 추가) |
| 고유 스킬 | 13종 | **33종** (희귀함 15 · 전설 5 신규) |
| 전설 패시브 | 5종 | **10종** — 새 축 5개(치명타 피해·빙결·공격속도·화상·방어 무시) |
| 특성 | 27종 | **61종** (새 특별함 34종 전부) |
| 버퍼 패시브 | 3종 | 5종 (골덕·럭키·마임맨 추가, 고라파덕은 흔함으로 내려가 제외) |
| 공격 연출 | 55종 | 150종 (새 95종은 역할·타입 규칙으로 생성, 개체마다 색·모양·크기 다름) |

히든(왕구리·뮤)·불멸·초월과 채팅 조합은 **다음 단계**다.

### 기존 55종 등급 정리

진화하면 반드시 등급이 오르도록 고쳤다.

| 포켓몬 | 전 → 후 | 이유 |
|---|---|---|
| 디그다 · 나옹 · 파라스 · 콘팡 | 특별함 → **흔함** | 진화형(닥트리오·페르시온·파라섹트·도나리)과 같은 등급이었다 |
| 고라파덕 · 망키 | 특별함 → **흔함** | 진화형 골덕·성원숭(특별함)이 생겼다 |
| 피카츄 · 모래두지 | 특별함 → **안흔함** | 강한 2단 계열(→ 희귀함) |
| 삐삐 · 푸린 | 흔함 → **안흔함** | 흔함에서 희귀함으로 두 단계 뛰던 것 |

공격력은 새 등급 기준으로 다시 잡았다. 옮겨 간 개체의 특성은 그대로 둔다.

### 조합식 규칙

| 결과 | 공식 |
|---|---|
| 진화 (한 단계) | 진화 전 ×2 — 캐터피 ×2 → 단데기 |
| 진화 (두 단계 이상 오름) | 진화 전 ×2 + **같은 타입 한 등급 아래 아무거나** — 쥬쥬 ×2 + 물 안흔함 → 쥬레곤 |
| 분기 진화 | 이브이 ×2 + 불꽃/물/전기 흔함 아무거나 → 부스터/샤미드/쥬피썬더 |
| 진화 전이 없는 포켓몬 | 같은 타입 아무거나 3칸 (한 등급 아래 둘 + 두 등급 아래 하나), 겹치면 조합을 바꿔 고유하게 |

**"아무거나" 재료(`any:타입:등급`)** 를 새로 만들었다. 판마다 등장 계열이 달라서 도우미를 특정 포켓몬으로
정하면 이번 판에 안 나와 막혔다(처음 정해진 도우미로 재 보니 클리어율 0%·도달 중앙 25). 타입+등급으로 바꾸자
도달 중앙이 33 → 38 로 올랐다. 조합식 화면에서는 타입 아이콘이 들어간 점선 칸으로 보인다.

### 판마다 등장 계열 추첨 (A안)

흔함이 44종이 되면 같은 흔함 2마리가 모일 확률이 1/3 로 떨어진다. 판을 시작할 때 **진화 계열**을 추첨해
소환 풀을 흔함 16 · 안흔함 14 · 특별함 14종 안팎으로 맞춘다(78계열 중 27~30계열).
- 소환은 이번 판 계열에서만 나온다.
- **이번 판에 없는 포켓몬도 조각 상점으로는 살 수 있다** — 조합식 재료에 ◆ 표시.
- 조합식 목록은 기본으로 **이번 판** 계열만(칩으로 전체 계열 보기).
- 시작 전 화면에 "이번 판 등장 29계열 · 흔함 17 · 안흔함 14 · 특별함 14종".

### 메타몽

모자란 **흔함 한 마리를 대신**한다. 조합식 하나에 한 마리, 흔함 칸에만. 대신한 칸에는 "메타몽" 표시가 붙는다.
아무거나 칸에는 들어가지 않는다(만능이 너무 만능이 되지 않게).

### 밸런스 (30판 × 2)

1단계 도중 클리어율이 0% 로 떨어졌다. 원인은 새 희귀함·전설의 스킬·패시브와 새 특별함의 특성이 없어서였다
(스킬이 처음 붙었을 때도 38% → 95% 였을 만큼 전력의 큰 몫이다). 스킬·패시브·특성·버퍼·연출을 모두 채운 뒤
`hpGrowth 1.153 → 1.109` 로 **40% · 57%** (합쳐 약 48%) 에 맞췄다. 조각 구매는 판당 12회 안팎으로 늘었다
(이번 판에 없는 재료를 조각으로 산다).

**보스 러시·챌린지·엔드리스와 쉬움·어려움·지옥은 다시 재지 않았다.**

### 생성 도구

수치·조합식·연출을 규칙으로 만든 스크립트를 `tools/gen1/` 에 남겼다. 로스터 정의는 `tools/gen1/roster.py`.
**이미 만든 데이터 위에 다시 돌리면 손으로 고친 부분을 덮어쓴다** — 새로 만들 때만 쓴다.

`uicheck` 총 126 · `redesigncheck` 108 — 전부 통과.

## 포켓몬 그림 다시 뽑기 · 골덕·강챙이 추가

### 잘린 그림의 원인 두 가지

1. **칸 가장자리를 4px 씩 잘라 내고 있었다.** 칸 테두리 선을 피하려던 것인데, 칸 끝까지 그려진
   포켓몬(윈디 꼬리 · 우츠보트 덩굴 · 딱구리 · 킹크랩 집게)의 끝이 함께 잘렸다. 1px 로 줄였다.
2. **몸 사이에 갇힌 체크무늬가 그대로 남았다.** 배경을 가장자리부터 지워 나가서, 덩굴 고리 안이나
   팔과 몸 사이처럼 윤곽선에 둘러싸인 곳은 닿지 못했다(우츠보트 고리 안 · 신뇽 몸 사이 등).
   → 둘러싸인 밝은 조각도 **체크무늬인지** 따져서 지운다:
   - 거의 모든 픽셀이 두 체크 색(±22) 근처이고,
   - 그 범위를 벗어난 음영이 거의 없고,
   - 밝은 칸 · 어두운 칸이 둘 다 섞여 있어야 한다.
   처음엔 "두 색이 섞였는가"만 봤더니 **후딘 숟가락 · 롱스톤 몸 · 레어코일 몸통**까지 구멍이 났다.
   은색·회색 음영은 체크 색보다 어둡거나 밝은 단계가 섞여 있어 둘째·셋째 조건에서 걸러진다.
   두 체크 색은 칸 테두리의 **밝은 무채색 픽셀만으로** 잡는다(테두리에 걸친 포켓몬 픽셀이 섞이면 틀어졌다).

### 골덕 · 강챙이

셋째 시트(96px 도트를 확대해 그린 두 칸)에서 잘라 넣었다. 이제 1세대 151종 그림이 전부 있다.

## 1세대 포켓몬 그림 95장 추가 (로스터 반영 전)

업로드한 시트 두 장에서 **96장**을 잘라 냈다(#056~#101 46장 · #102~#151 50장).
`mankey` 는 이미 있어 덮어쓰지 않았고, 나머지 **95장**을 `assets/pokemon/` 에 넣었다.
아직 `pokemon.js` 에 없어서 게임에는 나오지 않는다 — 조합식 작업 때 로스터에 넣는다.

### 잘라 낸 방법

- 두 시트 모두 배경이 투명이 아니라 **체크무늬가 그려진 그림**이었다.
- 칸 찾기: 밝은 무채색 덩어리 중 60px 이상을 칸으로 보고 줄·열 순서로 정렬. 두 시트 모두 정확히 46 · 50칸.
- 첫 시트(도트풍): 테두리에서 체크무늬 색을 따라 번지게 지운다. 도트 윤곽선이 막아 준다.
- 둘째 시트(일러스트풍)는 윤곽선이 부드러워 같은 방법으로는 **흰 몸통이 함께 지워졌다**
  (탕구리 해골·콘치·마임맨·뮤츠). 체크무늬가 12px 칸 · 235/205 두 색으로 정확했기에,
  **칸 위치마다 기대색을 계산해 그 색과 같은 픽셀만** 지우도록 바꿨다. 흰 몸통(255)은 기대색과 달라 남는다.
- 여백을 맞춰 가운데 정렬 후 96×96.

도구: `tools/extract_gen1_sheets.py`. 목록·주의 사항: `assets/pokemon/NEW_SPRITES.md`.

### 테스트판 크기

`build-tester.js` 가 `assets` 의 PNG 를 전부 넣고 있었다. 로스터에 있는 포켓몬 그림만 넣도록 바꿨다
(아직 안 쓰는 95장은 제외 — 테스트판 크기 그대로).

### 확인이 필요한 것

- 시트의 #062 는 강챙이가 아니라 **왕구리(politoed, 2세대)** 다. (발챙이=poliwag #060, 슈륙챙이=poliwhirl #061, 강챙이=poliwrath #062)
- 1세대 중 그림이 없는 것: **골덕(#055)**, **강챙이(#062)**.

## 모드 3종 재측정 (30판 × 2)

여러 세션 동안 특성·조각 가격·흔함 확률·버퍼 패시브가 바뀌는 사이 재지 않았던 모드를 다시 쟀다.

| 모드 | 재측정 | 조정 | 확정 |
|---|---|---|---|
| 보스 러시 (20) | **90%** — 크게 물러짐 | `hpMul 1.48 → 2.1` | **53% · 57%** · 도달 중앙 20 · 5.5분 |
| 챌린지 (50) | 17% · 23% | 없음 | 약 20% · 도달 중앙 42~44 · 12분 |
| 엔드리스 | 도달 중앙 46 · 52 (최대 64) | 없음 | 중앙 약 49 · 13~15분 |

보스 러시가 가장 많이 무너져 있었다. 매 라운드가 보스라 특성(분노·뿔드릴의 보스 피해)과
소환권(보스마다 1장)의 이득을 가장 크게 받는 모드다. 1.8배에서는 80%, 2.3배에서는 45% 로
사망이 10라운드 부근에 몰려서, 그 사이 2.1배로 두 번 재 55% 를 확인했다.

챌린지·엔드리스는 두 표본이 서로 맞고 목표 범위라 그대로 뒀다.

### 현재 기준선

| 모드 | 클리어 | 도달 중앙 | 판 시간 | 표본 |
|---|---|---|---|---|
| 일반 · 쉬움 | 87% | 50 | — | 30 |
| 일반 · 보통 | 50% | 49 | 14.2분 | 60 |
| 일반 · 어려움 | 27% | 44 | — | 30 |
| 일반 · 지옥 | 0% | 32 | — | 30 |
| 보스 러시 | 55% | 20 | 5.5분 | 60 |
| 챌린지 | 20% | 43 | 12분 | 60 |
| 엔드리스 | — | 49 (최대 64) | 14분 | 60 |

## 일반 모드 · 난이도 4단계 · 클리어 기록과 칭호

### 노멀 → 일반, 시작할 때 난이도 선택

모드 선택에서 **일반** 카드 안의 난이도 버튼으로 시작한다. 지금까지의 균형이 **보통**이다.

| 난이도 | 적 체력 | 라이프 | 보스 | 수입 | 봇 30판 클리어 | 도달 중앙 |
|---|---|---|---|---|---|---|
| 쉬움 | ×0.6 | +20 (80) | 약화 ×0.85 | +8% | **87%** | 50 |
| 보통 | ×1 | 60 | — | — | **50%** (60판) | 49 |
| 어려움 | ×1.3 | −10 (50) | 강화 ×1.15 | — | **27%** | 44 |
| 지옥 | ×1.7 | −20 (40) | 크게 ×1.3 | −8% | **0%** | 32 |

**요청 외에 더한 것**

- 적 체력만 올리면 어려움·지옥이 "오래 걸리는 보통"이 된다. **실수를 받아 주는 폭(라이프)** 과
  **보스 체력**, **수입**도 함께 움직였다. 쉬움은 반대로 넉넉하게.
- 기록이 **난이도별로 따로** 남는다(난이도 버튼마다 "클리어 N회 / 최고 R").
- 지옥은 무전략 봇이 한 번도 못 깬다(1.75배에서 한 번 10% 가 나왔지만 다시 재면 0%). 사람을 위한 난이도다.

구현: `RPD.effectiveMode(모드, 난이도)` 가 배율을 합친 "이번 판 모드"를 만든다. 체력·보스·수입·라이프를
쓰는 코드는 모두 `GameManager.mode` 만 보므로 한 곳에서 끝난다. 다른 모드에는 난이도가 붙지 않는다.
예전 저장의 노멀 기록은 "보통" 기록으로 옮겨 준다.

### 클리어 기록과 칭호

클리어 횟수(모든 모드 합계)에 따라 칭호가 오르고, **오른 칭호의 시작 보너스는 모두 쌓인다.**

| 클리어 | 칭호 | 시작 보너스 |
|---|---|---|
| 1 | 새내기 트레이너 | 골드 +30 |
| 3 | 성장하는 트레이너 | 소환권 +1 |
| 5 | 베테랑 트레이너 | 조각 +15 |
| 10 | 엘리트 트레이너 | 골드 +50 · 소환권 +1 |
| 20 | 챔피언 | 특별함 1마리 |
| 50 | 포켓몬 마스터 | 소환권 +2 · 조각 +30 |

난이도 칭호(보너스 없이 이름만): 어려움 첫 클리어 **도전자**, 지옥 첫 클리어 **지옥에서 돌아온 트레이너**.

- 모드 선택 맨 위에 칭호 · 총 클리어 · 다음 칭호까지 · 시작 보너스.
- 시작 전 화면에 "이번 판 시작 보너스 · 골드 +30".
- 클리어 화면에 "통산 N번째 클리어", 새 칭호가 생기면 축하와 함께 다음 판 보너스를 알린다.
- 칭호·보너스는 `js/data/titles.js` 에서 고친다.

시작 보너스는 반복 플레이를 조금씩 쉽게 만든다(의도). 자동 플레이는 보너스 없이 잰다.

`uicheck` 총 119.

## 설명서 (단축키 · 조각 상점)

- **게임 안**: 오른쪽 위 **?** 버튼 또는 **H** 키. 두 쪽(단축키·조작 / 조각 상점). Esc·바깥 클릭·× 로 닫는다.
  시작 화면에 "처음이라면 ? (또는 H)" 안내를 붙였다. 테스트판(단일 HTML)에도 그대로 들어간다.
- **파일**: `docs/설명서.md` — 같은 내용. 테스터에게 따로 보내거나 인쇄용.
- 설명서의 **가격·방출 조각·해금 라운드가 실제 게임 수치와 같은지** `uicheck` 가 검사한다.
  나중에 조각 가격을 바꾸고 설명서를 안 고치면 여기서 걸린다.

## 단축키 · 끌어놓기 · 타입별 보기 · 흔함 50% · 조각 가격 · 버퍼 패시브

### 1. 단축키와 끌어놓기

| 키 | 동작 |
|---|---|
| Space (Q) | 소환 |
| W | 강화 (고른 칸) |
| S | 창고로 (고른 칸) |
| F | 필드로 (보유 창에서 고른 포켓몬) |
| X · Delete | 방출 (고른 칸, 없으면 보유 창의 창고 개체) |
| C | 조합 (가장 좋은 것) |
| 1 · 2 · 3 | 배속 |
| P | 일시정지 |
| Esc | 선택 해제 · 창 닫기 |

단축키는 **버튼을 대신 눌러 준다** — 버튼이 잠겨 있으면 아무 일도 없어 규칙이 버튼과 같다.
입력칸·슬라이더에 초점이 있을 때는 가로채지 않는다. 버튼마다 작은 키 배지가 붙고,
스피커 버튼 안에 단축키 목록이 있다.

끌어놓기:
- **창고 → 필드**: 보유 칸(창고 배지가 붙은 것)을 끌어 필드 칸에 놓는다. 빈 칸이면 배치, 찬 칸이면 맞바꾼다.
- **필드 → 창고**: 필드 포켓몬을 끌어 보유 패널 위에 놓는다(패널에 "여기에 놓으면 창고로").

잡은 버그: 필드에서 끌다가 캔버스를 벗어나면 끌기가 끊겼다(`mouseleave` 가 취소하고 있었다).
놓는 곳이 필드도 패널도 아닐 때만 취소한다.

### 2. 타입별 묶어보기

보유 패널에 `타입별` 칩. 타입마다 묶고, 머리에 **필드에 올린 수 · 다음 시너지까지** 를 보여 준다
(`독 필드 2 · 2/3 → 독 피해 +25%`). 두 타입인 포켓몬은 두 묶음에 모두 나온다 — 시너지도 양쪽에 센다.
창고에 있는 것은 시너지에 안 들어간다는 점을 머리 숫자가 드러낸다.

### 3. 흔함은 50% 밑으로 내려가지 않는다

안흔함이 "같은 흔함 2마리"라 흔함은 끝까지 재료다. 25라운드 이후 흔함 34→24% 로 줄어
후반에 안흔함을 새로 못 만들었다. 확률표를 50% 로 고정하고, **해금 직후 보정·소프트 천장이
섞여도** 흔함 비중을 50% 로 받치는 규칙(`minCommonShare`)을 넣었다.
확정 천장·소환권은 "보장"이라 이 규칙 밖이다(실측 49.7~50.3%).

### 4. 조각 가격

| 등급 | 전 | 후 |
|---|---|---|
| 흔함 | 20 | **5** |
| 안흔함 | 46 | **10** |
| 특별함 | 108 | **25** |
| 희귀함·전설 | 245·550 | 그대로 |

판당 조각 구매 2회 → 7회 안팎.

### 5. 버퍼 패시브

특별함 이상 버퍼(고라파덕·픽시·푸크린)에게 공격력 오라와 다른 결의 패시브를 하나씩.
옆 칸(상하좌우·대각선) 아군만 받는다. 같은 종류는 더해지되 상한에서 멈춘다(공속 30% · 치명 20% · 치피 80%).

| 버퍼 | 패시브 | 효과 |
|---|---|---|
| 고라파덕 | 🌊 차분한 물결 | 주변 공격속도 +10% |
| 픽시 | 🌙 달빛 | 주변 치명타율 +8% |
| 푸크린 | 🎶 응원가 | 주변 공격속도 +8% · 치명타 피해 +30% |

버퍼 카드에 패시브가, 받는 쪽 카드에 "받는 버프 · 공속 +10%" 가 보인다. 데이터는 `js/data/auras.js`.

### 밸런스 (30판 × 2)

변경 뒤 50% · 80% (합쳐 65%). 싼 조각·버퍼 패시브·흔함 확보가 모두 플레이어 쪽 강화다.
`hpGrowth 1.146 → 1.153`, `waveClearBase 17 → 21`(초반 소환 1회 안팎 유지) 로 **47% · 53%** 에 맞췄다.

`uicheck` 총 109.

## 희귀함 특성 5종

희귀함은 이미 고유 스킬이 있다. 특성은 스킬과 겹치지 않는 결로, 특별함보다 한 단계 세게 줬다.

| 포켓몬 | 특성 | 효과 |
|---|---|---|
| 라이츄 | ⚡ 축전 | 공격 시 10% 주변 4마리 연쇄(공격력 80%) — 피카츄 감전의 강화판 |
| 독침붕 | 🐝 맹독침 | 공격 시 30% 독 2.5배 |
| 피죤투 | 🪶 순풍 | 공격속도 +15% |
| 고지 | 🦔 가시발톱 | 공격 시 20% 대상 방어력 -40 |
| 픽시 | 🍀 행운 | 치명타율 +12% |

버터플·라플레시아·푸크린은 스킬이 광역 제어라 특성까지 얹으면 넘친다 — 비워 뒀다.
새 종류 `armorBreakOnAttack`(방어력 깎기)과 상시 능력치 `critRateAdd`(치명타율)를 추가했다.
특성은 이제 **27종**.

### 밸런스 — 30판 한 번은 믿지 않는다

추가 직후 30판이 23% 로 나왔다. 특성은 힘을 더하기만 하는데 클리어율이 떨어진 것이라
30판을 한 번 더 돌렸더니 27%. 즉 직전에 확정했던 "40%" 는 **운 좋은 한 번**이었다.
`hpGrowth 1.152 → 1.146` 로 낮추고 30판씩 두 번 재 **40% · 40%** 를 확인했다(60판).

앞으로 수치를 확정할 때는 30판을 두 번 돌려 둘이 비슷한지 본다.

`uicheck` 총 101.

## 특성 확장 — 특별함 전부 · 아래 등급 일부

특성 4종 → **22종**. 특별함 12종은 전부, 안흔함은 14종 중 6종, 흔함은 16종 중 4종.
각 포켓몬의 모습·역할에 맞춰 골랐다.

| 등급 | 포켓몬 | 특성 | 효과 |
|---|---|---|---|
| 특별함 | 피카츄 | ⚡ 감전 | 공격 시 5% 주변 3마리 연쇄 |
| | 고라파덕 | 💧 멍함 | 12% 공격 대신 주변 60% 감속 |
| | 나옹 | 💰 금전운 | 처치 시 5% 추가 골드 |
| | 파라스 | 🍄 포자 | 20% 독 2배 |
| | 디그다 | 🕳️ 땅파기 | 12% 대상 1초 정지 |
| | 닥트리오 | 👥 삼연타 | 15% 한 번 더 공격 |
| | 망키 | 💢 분노 | 보스에게 25% 피해 2배 |
| | 파라섹트 | 💤 버섯포자 | 12% 대상 1.5초 잠재움 |
| | 페르시온 | 🗡️ 급소찌르기 | 치명타 피해 +50% |
| | 모래두지 | 🏜️ 모래바람 | 20% 대상 2초 40% 감속 |
| | 도나리 | 🦋 인분 | 15% 대상 주변 전부 독 |
| | 콘팡 | 👁️ 복안 | 사거리 +20% |
| 안흔함 | 이상해풀 | 🌱 광합성 | 라운드 시작마다 골드 +3 |
| | 리자드 | 🔥 맹화 | 체력 30% 이하 적에게 피해 +30% |
| | 어니부기 | 💦 물대포 | 8% 대상을 뒤로 밀어냄 |
| | 나인테일 | 👻 도깨비불 | 15% 화상 2배 |
| | 골뱃 | 🦇 초음파 | 8% 대상 1초 정지 |
| | 니드리노 | 🦏 뿔드릴 | 보스 피해 +20% |
| 흔함 | 캐터피 | 🕸️ 실뿜기 | 12% 대상 1.5초 30% 감속 |
| | 푸린 | 🎵 노래하기 | 5% 대상 1초 잠재움 |
| | 삐삐 | ✨ 손가락흔들기 | 8% 감속·독·연쇄 중 무작위 |
| | 꼬렛 | 🐭 앞니 | 공격속도 +10% |

### 규칙

- 종류가 4 → **13가지**로 늘었다(기절·감속·화상·밀치기·추가 공격·조건부 피해·무작위·라운드 골드·상시 능력치 등).
  `js/data/traits.js` 맨 위에 종류별 설명이 있어 숫자만 바꿔 새 특성을 만들 수 있다.
- **보스는 멈추지도 밀리지도 않는다.** 기절 특성 네 마리가 모이면 보스가 한 발짝도 못 움직였다.
- **추가 공격이 또 추가 공격을 부르지 않는다**(삼연타 무한 연쇄 방지).
- 확률 100% 인 상시 보너스(맹화·뿔드릴)는 매번 글자를 띄우지 않는다.

### 밸런스

특성이 늘자 노멀 30판 클리어율이 50% → **70%**. 수입을 줄이는 것만으로는 거의 움직이지 않아
(`killGoldPerWaves 12 → 15`, `waveClearPerWave 1.7 → 0.9` 로 63%), 체력 곡선을
`hpGrowth 1.142 → 1.152` 로 올려 **40%** 에 맞췄다. 사망이 30~54 전 구간에 퍼진다.

보스 러시·챌린지·엔드리스는 이번 변경 뒤 다시 재지 않았다.

`uicheck` +13(총 98).

## 특성 (일부 포켓몬만)

| 포켓몬 | 특성 | 효과 |
|---|---|---|
| 피카츄 | ⚡ 감전 | 공격 시 5% 확률로 주변 적 3마리에게 연쇄(공격력 60%) |
| 고라파덕 | 💧 멍함 | 12% 확률로 공격 대신 주변 적 60% 감속 2.5초 |
| 나옹 | 💰 금전운 | 처치 시 5% 확률로 추가 골드(8 + 라운드×0.6) |
| 파라스 | 🍄 포자 | 공격 시 20% 확률로 두 배 강한 독 |

- 데이터 `js/data/traits.js` — 맨 위에 추가·수정 방법이 적혀 있다. 종류 네 가지 중 하나를 고르고 숫자만 바꾸면 된다.
- 실행 `js/systems/TraitManager.js` — CombatManager 는 공격 전·후 두 곳에서만 부른다.
- **"공격 시" 확률은 공격 한 번에 한 번만 굴린다.** 피카츄는 원래 한 번에 적 4마리를 때리는 연쇄형이라
  타격마다 굴리면 적힌 5% 가 실제로는 20% 가까이 된다. 검사가 이것을 감시한다.
- 칸 정보 카드 · 보유 상세창 · 조합식 창에 특성이 보이고, 발동하면 필드에 `⚡ 감전` 처럼 뜬다
  (같은 개체는 0.6초에 한 번만 — 글자가 겹치지 않게). 멍함은 퍼지는 물결로 범위를 보여 준다.

밸런스: 노멀 30판 60%(직전 50%). 30판 표본의 흔들림(±15%p) 안이라 수치는 건드리지 않았다.

`uicheck` +8(총 104).

## 이름 수정

`rattata` 꼬리선 → **꼬렛**, `raticate` 다꼬리 → **레트라**.
`js/data/pokemon.js` 의 이름·설명, 조합식 주석, 조합식 안내 문서의 id 표를 함께 고쳤다.
id 는 그대로라 조합식·그림·저장된 도감 기록은 영향이 없다.
(이 문서의 과거 기록 속 옛 이름은 당시 기록이라 그대로 둔다.)

## 테스터용 단일 파일

`index.html` 은 스크립트 46개·CSS 3개·그림 104장을 옆 폴더에서 읽는다. 그 파일만 보내면 빈 화면이다.
`tools/build-tester.js` 가 전부를 **HTML 한 파일**에 넣는다(약 2.3MB).

- CSS·JS 는 `<style>`·`<script>` 로 인라인. 코드 안의 `</script>` 는 끊기지 않게 바꾼다.
- 그림은 data URL 로 모아 `window.RPD_INLINE` 에 넣고, `Assets.url()` 이 경로를 그쪽으로 돌린다.
  평소(폴더째 열 때)에는 경로 그대로라 개발 방식은 바뀌지 않는다.
- 외부 파일 참조가 하나라도 남으면 빌드가 실패한다.
- 제목에 빌드 시각을 붙인다(`테스트판 2026-09-21 09:37`).
- 원본은 읽기만 한다. 결과는 `dist/` 에 새로 쓴다.

빈 폴더에 그 파일 하나만 두고 열어 확인했다 — 실패한 요청 0, 오류 0, 포켓몬·적·UI 그림 전부 로드.

### 같이 고친 것

적 스킨 그림이 읽히는 첫 순간에 원래 경로(`assets/enemies/grunt.png` — 원래 없는 파일)를
요청하고 있었다. 게임은 코드 생성 그림으로 넘어가 멀쩡했지만 매 판 404 가 났다.
스킨이 읽히는 동안은 파일을 요청하지 않고 바로 생성 그림을 쓴다.

## 안흔함 조합식 = 같은 흔함 2마리

### 왜

`캐터피 + 뿔충이 → 단데기` 와 `뿔충이 + 캐터피 → 딱충이` 가 **같은 재료로 결과가 둘**이었다.
둘을 모아 조합하면 무엇이 나올지 알 수 없었다. 조합식 하나를 보고 계획을 세우는 게임에서
이건 치명적이다.

### 무엇을 바꿨나

안흔함 14종을 전부 **진화 전 모습 ×2** 로 바꿨다. 단데기 = 캐터피 ×2, 리자드 = 파이리 ×2 …
결과가 하나로 정해지고, 무엇을 모아야 하는지 이름만 봐도 안다.

| 곳 | 표시 |
|---|---|
| 조합식 줄 | 그림 하나에 `×2` 배지 + `1/2` 보유 수 |
| 조합식 창 · 보유 상세창 | 같은 방식(`캐터피 ×2`) |

조합 판정은 원래 같은 종 여러 마리를 셀 수 있었지만, 화면 표시가
"재료마다 1마리"를 전제로 해서 `1/2` 상태를 둘 다 "없음"으로 그리고 있었다. 재료 칸마다
따로 세도록 고쳤다.

### 규칙 검사

`같은 종을 2개 요구하지 않는다` 검사를 없애고 셋으로 바꿨다.

- 안흔함은 같은 흔함 2마리로 만든다
- **같은 재료로 서로 다른 결과가 나오지 않는다** (이번 문제의 재발 방지)
- 특별함 이상은 같은 종을 두 번 요구하지 않는다

재료 이름을 적어 두고 있던 검사 4개(조각 추천·구매 후 조합)도 조합식에서 재료를 꺼내 쓰게 고쳤다.
이제 조합식을 바꿔도 검사가 따라온다.

### 밸런스

버려지던 중복 소환이 전부 재료가 되면서 조합이 훨씬 잘 된다.
자동 플레이 30판 클리어율 **40% → 73%**. 후반 처치 골드(`killGoldPerWaves 9 → 12`)를 줄여
**50%** 로 되돌렸다(도달 중앙 50, 판 시간 13.9분).

### 직접 바꾸는 법

`js/data/recipes.js` 맨 위에 방법이 적혀 있고, 각 줄 오른쪽에 한글 이름이 붙어 있다.
자세한 안내와 포켓몬 id 표는 `docs/RECIPES_GUIDE.md`.

### 같이 고친 것

조합식 줄의 가운데를 누르면 조합이 되지 않고 창만 떴다. 결과 칸 전체가 클릭을 받고 있었다.
그림과 이름을 누를 때만 창이 뜨고, 나머지는 예전처럼 조합한다.

## 소환 누출 수정 · 조합식 창 · 보스 보상

### 1. 소환에서 희귀함·전설이 새어 나오고 있었다

"소환은 특별함까지"로 바꿨는데 실제로 뽑아 보니 **새어 나왔다.** 경로가 둘이었다.

| 경로 | 원인 | 누출 |
|---|---|---|
| 천장(확정) | 천장이 "소환 가능한 등급"이 아니라 "해금된 등급 전체"를 봤다 | 25R 이후 40회마다 희귀함, 33R 이후 70회마다 전설 (약 2.5%) |
| 소환권 | 보장 등급을 해금 등급 전체에서 골랐다 | 33R 이후 소환권이 희귀함을 확정으로 뽑을 수 있었다 |

둘 다 `summonable` 등급만 보게 고쳤다. 6,000회를 굴려 0회인 것을 검사가 확인한다.

같이 발견한 것: **소환권이 쌓이기만 하고 한 번도 쓰이지 않았다.** 버튼은 "소환권 N"을 보여 주면서
정작 소환은 골드를 요구했다(봇도 같았다). 이제 소환권이 있으면 먼저 쓴다.

### 2. 조합식의 포켓몬을 누르면 그 포켓몬의 조합이 뜬다

조합식 줄의 재료·결과 그림을 누르면 창이 뜬다.

- **만드는 법** — 재료 그림, 보유 수, 부족 여부. 완성 가능하면 [지금 조합하기].
- 재료 그림을 다시 누르면 **그 재료를 만드는 법으로 내려간다**(`조합 ›` 표시). [‹] 로 되돌아온다.
  전설을 목표로 잡고 희귀함 → 특별함 → … 으로 계획을 세울 수 있다.
- **재료로 쓰이는 곳** — 이 포켓몬이 들어가는 결과들(누르면 그쪽으로 이동).

줄의 나머지 부분을 누르면 예전처럼 조합한다.

### 3. 보스 보상

| 보스 | 보상 |
|---|---|
| 10R | 특별함 유닛 1 |
| 20R | 소환권 3 + 특별함 유닛 1 |
| 30R | 희귀함 유닛 1 |
| 40R | 소환권 3 + 희귀함 유닛 1 |
| 50R | 전설 유닛 1 (엔드리스에서만 의미가 있다) |
| 60R~ | 소환권 3 + 희귀함 유닛 1 (10라운드마다) |

- 유닛의 종은 조합 보정을 따른다 — 필요한 재료일 확률이 높다.
- 10의 배수 라운드 보스만 센다. 보스 러시(매 라운드 보스)도 10·20R 에만 받는다.
- 자리가 없으면 그 등급 조각 값만큼 **조각으로 바꿔 준다** — 보상이 조용히 사라지지 않는다.
- 필드 오른쪽 위에 **다음 보스 보상 예고**, 받을 때 가운데에 보상 창과 효과음.

### 밸런스

누출을 막은 만큼(판당 희귀함·전설 몇 마리) 보스 보상이 되돌려 줘서 노멀은 거의 그대로다.

| 모드 | 전 | 후 (30판) | 조정 |
|---|---|---|---|
| 노멀 | 43% | **40%** · 사망 25~49 전 구간 | 없음 |
| 보스 러시 | 57% | 73% → **57%** | `hpMul 1.4 → 1.48` (소환권이 실제로 쓰이면서 보스마다 1장씩 들어온다) |

챌린지·엔드리스는 이번에 다시 재지 않았다.

### 검사

`uicheck` +9(총 87). 천장·소환권으로 희귀함·전설이 안 나오는지, 소환권이 골드 없이 쓰이는지,
보스 보상이 요청한 표대로인지, 10의 배수에만·한 번만 주는지, 자리가 없으면 조각으로 바뀌는지,
조합식 창의 내려가기·뒤로가기가 살아 있는지.

## 적 몬스터 디자인 적용

업로드한 디자인 시트 한 장에서 **49장의 스프라이트를 잘라 냈다.**
시트는 이미 배경이 투명이라, 카드마다 아래쪽 등급 표식(일반·정예·보스)을 기준점으로 잡고
그 위의 그림 영역에서 불투명 픽셀만 모았다. 제목 글자 잔해는 걸러 낸다.
일반·정예는 96px, 보스는 128px 정사각형. `assets/enemies/digimon/`.

### 성질은 그대로, 겉모습만

적의 성질(질풍·육중·철갑·군집·재생·분열·보막)은 전투 설계의 핵심이라 건드리지 않았다.
`data/enemyskins.js` 가 **라운드 구간 × 성질 → 그림·이름** 만 정한다.
이 파일을 지워도 전투 결과는 한 자리도 바뀌지 않는다 — `uicheck` 가 이것을 감시한다.

| 성질 | 그림 | 구간별 계열 |
|---|---|---|
| 잡졸 | 기본 | 1~9 코로몬 · 11~19 뿔몬 · 21~29 아구몬(청) |
| 질풍 | 날개(비행)형 | 31~39 아그몬 · 41~ 테일몬 |
| 육중 | 강화형 | |
| 무리 | 군체 | |
| 분열체·파편 | 돌연변이/속성형 | |
| 철갑·재생체·보막 | 정예 3종 | 아구몬 → 파피몬 → (이어 씀) → 데블몬 |

정예가 없는 구간(21~29 · 41~)은 바로 앞 구간의 정예를 이어 쓴다.

보스: 10R 그레이몬 · 20R 가루몬 · 30R 메탈그레이몬(보스 성질에 따라 기본/강화/특수) ·
40R 피에몬 · 50R 오메가몬(최종형). 엔드리스 50R 이후는 오메가몬 변형을 돌려 가며 쓴다.

### 그리기

- 판정 크기(`e.size`)는 그대로 두고 **그림만 1.6배**(무리 1.9배, 보스 1.55배)로 그린다.
- 원본이 고해상도라 크게 줄여 그리므로 부드러운 축소를 쓴다(포켓몬 도트는 계속 선명하게).
- 오른쪽으로 걸을 때 좌우를 뒤집는다.
- 파일이 없으면 예전처럼 코드 생성 그림으로 돌아간다.
- 보스 체력바를 라운드 알약 아래(y 56)로 내렸다. 둘이 화면 위 가운데에서 겹쳐 있었다.

### 쓰지 않은 그림 4장

정예 군체 3장(아구몬·파피몬·데블몬)과 메탈그레이몬(군체)은 대응하는 적 성질이 없어
파일만 넣어 두었다. 정예 무리 같은 성질을 새로 만들면 바로 쓸 수 있다.

### 검사

`uicheck` +5(총 78). 파일이 전부 있는지, 모든 구간·모든 성질에 그림이 붙는지,
보스가 10라운드마다 다른지, 스킨이 체력·속도·방어·크기를 바꾸지 않는지.

## 배경음과 효과음

**음원 파일을 쓰지 않는다.** 스프라이트를 코드로 그리는 것과 같은 이유다 —
index.html 을 더블클릭해 여는 게임이라 파일이 하나라도 없으면 그 자리에서 깨진다.
WebAudio 로 그때그때 합성하면 받을 것도, 깨질 것도 없다.

### 효과음 19종

소환(등급별 3종) · 조합(일반/고등급) · 배치 · 강화 · 방출 · 칸 구매 · 창고 확장 ·
조각 구매 · 스킬 · 처치 · 처형 · 보호막 파괴 · 라이프 감소 · 라운드 시작 ·
보스 등장 · 승리 · 패배 · 거부 · 버튼 클릭.

게임 코드는 소리를 모른다. `AudioManager` 가 이벤트만 듣는다 —
소리를 통째로 지워도 게임 로직은 한 줄도 바뀌지 않는다.

### 배경음 3곡

평시 · 전투 · 보스. 16스텝 시퀀서에 베이스·리드·패드를 깔았다.
`game:state` 와 `boss:appeared` 로 곡이 갈아 끼워진다.
0.2초 앞을 미리 예약해 `setInterval` 의 흔들림이 박자에 새지 않게 했다.

### 귀가 아프지 않게

후반 라운드는 초당 수십 마리가 죽는다. 그대로 내면 소음이 된다.

- 효과음마다 **쿨다운**을 둔다(처치음은 0.055초 — 초당 18회가 상한).
- **동시 발음 14개** 상한. 넘으면 새 소리를 내지 않는다.
- 처치음은 음높이를 조금씩 흔들어 같은 소리의 반복을 줄인다.
- 탭이 가려지거나 일시정지면 **스케줄러를 멈춘다.** 배터리를 쓸 이유가 없다.

### 조작

상단 스피커 버튼에 배경음·효과음 슬라이더와 음소거가 들어 있다.
브라우저는 사용자가 한 번 누르기 전에는 소리를 못 내므로 첫 입력에서 오디오가 깨어난다.
음량과 음소거는 저장돼 다음 판에도 유지된다.

### 구현하다 잡은 것

첫 판 배경음이 통째로 안 나왔다. 소리가 깨어나기 전에 `game:state` 이벤트가 이미
곡을 'calm' 으로 정해 둬서, 정작 시작할 때 `setTrack` 이 "같은 곡"이라며 아무것도 하지 않았다.
시작 시점에 스케줄러를 직접 돌리도록 고쳤다.

### 검사

`uicheck` 에 8개 추가(총 65개). 음원 파일을 참조하지 않는지, AudioContext 가 없는
환경(노드)에서도 터지지 않는지, 첫 입력 전에는 소리를 내지 않는지, 모든 효과음에
쿨다운이 있는지, 일시정지·탭 전환에 멈추는지, 주요 사건 9종에 소리가 붙어 있는지.

## 모드 3종 재측정 (규칙 변경 이후)

라이프 60 과 수입 하향이 모든 모드에 그대로 걸리므로 다시 쟀다. 표본은 모드마다 30판이다.

| 모드 | 전(변경 전) | 후 | 조정 |
|---|---|---|---|
| 보스 러시 (20) | 57% · 중앙 20 | 67% → **57%** | `hpMul 1.22 → 1.4` |
| 챌린지 (50) | 13% · 중앙 33 | **17% · 중앙 43** | 없음 |
| 엔드리스 | 중앙 30 (최대 59) | **중앙 51 (최대 64)** | 없음 |

### 보스 러시만 손봤다

라이프가 3배가 되면서 67% 로 물러졌다. 보스 체력 배수를 올려 57% 로 되돌렸다.
사망이 10~19 구간에 몰리는데, 매 라운드가 보스인 모드라 초반에 죽는 판이 없는 것은 자연스럽다.

### 챌린지·엔드리스는 두었다

챌린지 17% 는 "가장 어려운 모드"로 의도한 범위이고, 도달 중앙이 33 → 43 으로 올라
라이프 덕에 중간 결과가 더 잘 남는다.

엔드리스는 중앙 51 · 최대 64 로 노멀(중앙 48)을 넘어섰다. 끝이 없는 모드가
노멀보다 짧던 예전 문제가 해소됐다. 다만 판 시간이 14.8분으로 가장 길다 —
더 길어지면 보스 주기(7)를 당겨 끊는 것을 검토할 것.

### 현재 기준선 (표본 30판)

| 모드 | 클리어 | 도달 중앙 | 판 시간 |
|---|---|---|---|
| 노멀 (50) | 43% | 48 | 13.4분 |
| 보스 러시 (20) | 57% | 20 | 5.6분 |
| 챌린지 (50) | 17% | 43 | 11.9분 |
| 엔드리스 | — | 51 (최대 64) | 14.8분 |

## 플레이 피드백 5건 — 소환 상한 · 라이프 · 시너지 확장

이번 변경은 규칙을 건드린다. 다섯 가지가 서로 맞물려 난이도가 크게 흔들려서,
마지막에 경제로 다시 잡았다.

### 1. 출구 구간에 칸을 열었다

기본 격자가 x685 에서 끝나 **마지막 직선 250px 를 아무도 덮지 못했다.**
"출구 앞에서 놓친다"가 구조적으로 생기던 자리다. 경로 위아래로 기본 2칸(`860,390` · `960,520`),
확장 2칸(680G · 980G)을 놓았다. 경로 중심에서 65px — 최소 52px 규칙을 지킨다.

### 2. 조합식의 ??? 제거

랜덤 디펜스는 "어떤 전설을 향해 갈지" 정하고 재료를 모으는 게임인데,
목적지가 `???` 면 계획 자체가 불가능하다. 결과 이름과 그림을 처음부터 보여 준다.
발견의 재미는 첫 조합 때의 연출이 맡는다(도감 실루엣은 그대로 둔다).

### 3. 라이프 20 → 60

후반 한 라운드 적이 26~30마리다. **아무것도 못 잡아도 두 라운드는 버틴다.**

### 4. 시너지 7종 → 11종

로스터에 가장 많은 타입인데 시너지가 없던 넷을 채웠다.

| 타입 | 보유 종수 | 효과 |
|---|---|---|
| 독 | 21 | 독 피해 +25/55/95% · 최대 스택 증가 (3·6·9마리) |
| 노말 | 11 | 치명타율 +10/20/32% · 치명타 피해 (3·6·9마리) |
| 벌레 | 10 | 소환 비용 -10/20/32% (2·4·6마리) |
| 페어리 | 4 | 버프 효과 +30/60% · 라이프 보호막 (2·4마리) |

효과는 전부 기존 보너스 키를 쓴다 — 전투 코드는 한 줄도 바뀌지 않았다.

### 5. 소환은 특별함까지

| 라운드 | 전 | 후 |
|---|---|---|
| 33~ | 흔함 22 · 안흔함 25 · 특별함 26 · **희귀함 20 · 전설 7** | 흔함 24 · 안흔함 34 · **특별함 42** |

희귀함·전설은 **조합식과 조각 상점으로만** 얻는다. 뽑기로 전설이 나오면
조합식을 향해 계획을 세울 이유가 사라진다. `Tiers` 에 `summonable` 플래그를 넣고,
"해금 라운드에 그 등급이 나온다" 검사는 소환 가능한 등급에만 적용하도록 고쳤다.

### 난이도 재조정 — 물량도 체력도 아닌 수입이었다

다섯 변경 뒤 무전략 봇 클리어율이 **83%** 가 됐다(라이프 3배 + 시너지 4종 + 출구 칸).
되돌리려고 세 가지를 시도했다.

| 시도 | 결과 | 왜 |
|---|---|---|
| 체력 배율 상향 | 83% → 83% | 후반 체력은 예나 지금이나 병목이 아니다 |
| 방어력 상향 | 83% | 관통·방어 감소가 곳곳에 있어 묻힌다 |
| 물량 +25% | 83% → **96%** (더 쉬워졌다) | 적이 많을수록 처치 골드가 늘어 보드가 더 강해진다 |
| **수입 -25%** | 83% → **43%** | 보드 전력이 곧 수입이었다 |

`killGoldPerWaves 5 → 9` · `waveClearBase 25 → 17` · `waveClearPerWave 3 → 1.7`.
물량을 늘리면 오히려 쉬워진다는 것이 이번에 확인한 가장 큰 사실이다.

### 결과 (30판)

| 지표 | 값 |
|---|---|
| 클리어율 | **43%** |
| 도달 라운드 | 최소 29 · 중앙 48 |
| 판 시간 | 13.4분 |
| 사망 분포 | 25~29:2 · 30~34:2 · 35~39:1 · 40~44:5 · 45~49:6 |

**25~49 의 모든 구간에 사망이 있다.** 이 프로젝트에서 처음이다.
수입을 더 줄이면 초반이 답답해지고(`초반에 라운드당 1~2회 살 수 있다` 검사가 하한을 지킨다),
더 풀면 후반이 무너지지 않는다. 지금이 두 검사 사이의 좁은 자리다.

## 테두리 규칙 정리 · 보유 정렬

| 문제 | 원인 | 수정 |
|---|---|---|
| 조합식에 빨간 테두리가 떠서 등급과 헷갈린다 | 부족한 재료를 빨간 테두리로 표시했다. 등급 테두리 위에 겹쳐 색이 두 겹이 됐다 | **테두리는 등급 전용.** 부족한 재료는 그림을 흐리게 하고 숫자를 죽여서 말한다 |
| 창고 칸에 색이 두세 가지 섞여 나온다 | 등급 테두리 + 재료 초록 링 + 창고 점선까지 한 칸에 얹혀 있었다 | 테두리는 등급 하나. **재료는 초록 점**, 창고는 오른쪽 위 `창고 N` 배지 |
| 보유 목록 정렬을 고를 수 없다 | 필드 먼저 하나뿐이었다 | **필드 먼저 · 등급순 · 수량순 · 이름순** 네 가지 |

### 규칙 하나로 줄인 이유

색은 한 가지만 말해야 한다. 등급을 테두리로 읽기로 정한 이상, 다른 상태(부족·재료·창고)는
테두리가 아닌 수단으로 말해야 한다. 흐림·점·배지·정렬 순서가 그 자리를 대신한다.

### 정리하다 발견한 것

보유 정렬 코드와 정렬 칩이 **두 벌 들어가 있었다.** id 가 같은 요소가 둘이라
아래쪽 칩 줄은 클릭이 아예 먹지 않는 상태였다. 한 벌로 합쳤고,
`uicheck` 가 "정렬 상태·처리·요소가 중복이면 실패"를 감시한다.

## 밸런스 재측정 (표본 30~40판)

지금까지의 수치는 대부분 12~24판 표본이었다. 같은 설정에서 20판 클리어율이
19% · 40% · 50% 로 나오는 것을 확인한 뒤, **표본을 늘려 전부 다시 쟀다.**
아래 값이 현재 기준선이다.

| 모드 | 표본 | 클리어 | 도달 중앙 | 판 시간 | 사망 분포 |
|---|---|---|---|---|---|
| 노멀 (50) | **40판** | 43% | 44 | 12.2분 | 20~24:1 · 30~34:11 · 40~44:11 |
| 보스 러시 (20) | **30판** | 57% | 20 | 5.4분 | 5~9:5 · 10~14:3 · 15~19:5 |
| 챌린지 (50) | **30판** | 13% | 33 | 8.8분 | 20~24:12 · 30~34:8 · 40~44:6 |
| 엔드리스 (무한) | **30판** | — | 30 (최대 59) | 8.1분 | 20~24:14 · 30~34:10 · 45~54:4 |

### 조정한 것

보스 러시만 손봤다. 30판으로 재 보니 37% 였다(14판 표본에서는 50% 로 보였다).
`hpMul 1.3 → 1.22` 로 낮춰 **57%** 에 맞췄다. 사망이 5~19 전 구간에 고르게 퍼진다.

나머지 셋은 그대로 뒀다. 노멀 43% 는 목표대로고, 챌린지 13% 는 "가장 어려운 모드"로
의도한 범위이며, 엔드리스는 끝이 없는 모드라 클리어율 자체가 지표가 아니다.

### 남은 관찰

노멀의 사망이 **30~34 와 40~44 두 덩어리**로 나뉜다(25~29 와 35~39 는 비어 있다).
각각 특별함→희귀함 전환과 마지막 관문이라 이유는 설명되지만, 완전히 매끄럽지는 않다.
다만 이 정도 차이는 40판에서도 판별이 쉽지 않아, 다음에 손댈 때는 더 큰 표본이 필요하다.

**교훈: 20판 표본으로 ±15%p 를 신호로 읽지 말 것.** 이 문서의 과거 수치 중
12~24판으로 잰 것들은 그 폭 안에서 흔들리는 값이다.

## 플레이 피드백 반영 — 창고 · 조합식 · 등급 구분

실제로 해 보고 받은 여섯 가지. 전부 "보이지 않아서" 또는 "할 수가 없어서" 생긴 문제였다.

| 문제 | 원인 | 수정 |
|---|---|---|
| 창고 포켓몬이 뒤섞여 보인다 | 보유 목록이 필드/창고를 구분 없이 등급·수량으로만 정렬했다 | **필드에 나가 있는 개체가 먼저**, 창고에만 있는 개체는 뒤. 창고 전용 칸은 점선 테두리로 구분 |
| 필드가 꽉 차면 아무것도 못 한다 | 필드 → 창고로 보내는 길이 UI 에 없었다 | 하단에 **[창고로]** 버튼. 남은 칸 수가 버튼에 뜬다 |
| 조합식이 중구난방 | 완성 가능 순으로만 섞여 있었다 | **결과 등급 순(전설 → 안흔함)으로 정렬** + 등급 칩으로 좁히기 |
| 창고 포켓몬을 버릴 수 없다 | `EconomyManager.sell()` 이 필드 칸만 받았다 | `sellStored()` 추가. 보유 상세창에서 바로 방출 |
| 필드에서 등급이 안 보인다 | 모서리 보석 하나뿐이었다 | **칸 테두리 색 = 등급.** 보유·조합식·상점·도감도 같은 규칙으로 통일 |
| 조합식을 찾기 어렵다 | 상세창이 결과 이름만 보여 줬다 | 상세창에 **재료를 전부 펴서** 보여 주고(가진 것 초록·없는 것 빨강·자기 자신 금색), 아래 조합식 목록도 그 개체가 들어가는 것만 남긴다 |

### 등급 테두리 규칙

필드 칸은 짙은 남색 바깥선 위에 등급색 테두리를 얹는다(밝은 풀밭에서도 색이 죽지 않게).
보유 칸·조합식 재료·상점 줄·도감 칸이 모두 같은 색을 쓴다.
화면 어디를 보든 **테두리 색 하나로 등급을 읽을 수 있다.**

### 조합식 정렬

등급 순으로 묶고, 같은 등급 안에서는 완성 가능한 것 → 재료가 덜 모자란 것 순이다.
칩에 등급별 개수가 함께 뜬다(`안흔함 14 · 특별함 12 · 희귀함 8 · 전설 5`).
보유 포켓몬을 누르면 `닥트리오 재료 조합식 ×` 칩이 붙고 목록이 그만큼 좁아진다.

### 좁은 화면

버튼이 하나 늘고 칩 줄이 생겨 1366×768 에서 조합식 헤더가 잘렸다.
헤더의 개수 표시를 숨기고 버튼 여백을 줄여 다시 한 화면에 넣었다.

### 검사

`bootsmoke` +4 · `uicheck` +4. 창고로 보내기, 창고 방출, 등급 순 정렬, 창고 전용 개체가
뒤로 가는지, 등급 테두리가 다섯 곳에 다 있는지, 상세창-조합식 연동이 살아 있는지.
도구 5종 전부 통과.

### 밸런스 영향

전투 규칙은 바뀌지 않았다(UI 와 창고 조작만). 자동 플레이 20판 40% 클리어.
다만 같은 설정에서 16~24판 표본의 클리어율이 19~50% 로 흔들린다 —
**표본 20판으로는 ±15%p 를 신호로 착각하기 쉽다.** 수치를 다시 만질 때는 40판 이상을 쓸 것.

## 조각 상점 (빠져 있던 기능)

조각은 "재료가 끝내 안 나오는 판"을 구제하는 유일한 장치인데(기획 §19),
**모이기만 하고 쓸 화면이 없었다.** `ShardManager.suggestions()` 와 `buy()` 는 구현돼 있었고
테스트도 통과했지만, 그 API 를 부르는 건 자동 플레이 봇뿐이었다.
밸런스를 봇 기준으로만 검증해 온 탓에 다섯 번의 작업 동안 계속 가려져 있었다.

### 어디에 넣었나

별도 창을 만들지 않고 **조합식 패널의 탭**으로 넣었다.
조각을 쓰고 싶어지는 순간은 "조합식을 보다가 재료가 하나 모자란 걸 알았을 때"이고,
그 자리에서 끝나야 한다.

- `[전체] [완성가능] [미완성] [◆ 조각 상점]`
- **조합에 바로 필요한 재료가 맨 위**에 오고 초록 테두리가 붙는다. 그다음이 "곧 필요".
- 못 사는 줄도 보여 주되 이유를 적는다 — `27라운드부터` / `조각 부족` / `자리 없음`.
  무엇이 열릴지 보이지 않으면 조각을 모을 이유가 생기지 않는다.

### 필드가 차면 창고로

`buy()` 가 필드 빈 칸만 보고 있어서, 필드가 꽉 차는 후반에는 상점이 통째로 잠겼다.
정작 조각이 가장 필요한 시점이다. 소환과 같은 규칙(필드 → 없으면 창고)으로 맞췄다.

### 가격 재조정

상점이 실제로 열리자 판당 구매가 1.0 → **10.5회**로 뛰었다. 그 수준이면 구제책이 아니라
상점이고, "재료가 안 나온다"는 긴장이 사라진다. 가격을 두 단계에 걸쳐 약 70% 올렸다.

| | 전 | 후 |
|---|---|---|
| 가격 (흔함~전설) | 12 / 28 / 65 / 150 / 340 | **20 / 46 / 108 / 245 / 550** |
| 판당 구매 | 10.5회 | **5.4회** |

체력 배율은 1.140 → **1.142** 로 미세 조정했다.

### 결과 (24판)

| 지표 | 값 |
|---|---|
| 클리어율 | 50% |
| 도달 라운드 | 최소 23 · 중앙 50 |
| 판 시간 | 13.7분 |
| 사망 분포 | 20~24:3 · 30~34:3 · 40~44:6 |

### 검사

`uicheck` 에 7개(총 51개), `bootsmoke` 에 클릭 경로 1개를 넣었다.
조각 부족·미해금 등급·자리 없음에서 거부되는지, 거부됐을 때 조각이 줄지 않는지,
필드가 차면 창고로 가는지, 추천 목록에 "하나만 모자란 재료"가 뜨는지.

## 데이터 정리와 난이도 재분배

리디자인·스킬 작업 내내 "원본 데이터 문제"로 미뤄 뒀던 것들을 정리했다.
결과로 **검증 도구 다섯 개가 전부 통과한다**(이 프로젝트에서 처음이다).

### 등급이 역전된 조합식 6개

결과보다 재료가 더 높은 등급이던 것들이다. 예를 들어 어니부기(안흔함)를 만들려면
고라파덕(특별함)이 필요했다 — 상위 개체를 갈아 하위 개체를 만드는 셈이라
조합식을 보는 사람 입장에서 앞뒤가 맞지 않는다.

재료를 등급 규칙(`결과-1` 두 개 + `결과-2` 하나)에 맞게 바꿨다.
누가 어떤 재료로 얼마나 쓰이는지 세어, **가장 덜 쓰이는 개체**를 넣어 수요를 고르게 했다.

| 결과 | 전 | 후 |
|---|---|---|
| 어니부기 | 꼬부기 + **고라파덕(T3)** | 꼬부기 + 니드런♂ |
| 다꼬리 | 꼬리선 + **나옹(T3)** | 꼬리선 + 이상해씨 |
| 골뱃 | 주뱃 + **고라파덕(T3)** | 주뱃 + 아보 |
| 닥트리오 | **디그다(T3)** + 꼬리선 + 깨비참 | 아보크 + 깨비드릴조 + 깨비참 |
| 페르시온 | 다꼬리 + 나인테일 + **나옹(T3)** | 다꼬리 + 나인테일 + 푸린 |
| 픽시 | 삐삐 + **푸크린(T4)** + 고라파덕 | 망키 + 페르시온 + 골뱃 |

### 켜지지 않는 시너지 3종

드래곤·에스퍼·고스트는 **로스터에 개체가 하나도 없어** 영원히 0이었다.
우측 패널에 켜질 수 없는 줄이 세 개 남아 "내가 못 모은 것"처럼 보였다. 목록에서 뺐다.
타입 효과 자체(`TypeParams`)는 그대로라 그 타입 포켓몬을 추가하면 항목만 되살리면 된다.

전기(2종)·격투(1종)는 남겼다. 같은 종을 여러 마리 올릴 수 있으므로 도달 가능하다 —
검사도 "종 수"가 아니라 "필드에 그만큼 올릴 수 있는가"로 고쳤다.

### 사망이 30~34에 몰리던 이유

40판 내내 사망의 80% 이상이 30~34 라운드였다. 적 구성을 바꿔도, 체력 배율을 바꿔도
그대로였다. 곡선을 뽑아 보니 **30라운드 보스 한 마리가 원인**이었다.

| 라운드 | 필요 DPS |
|---|---|
| 29 | 5.5K |
| **30 (보스)** | **11.0K** |
| 31 | 7.6K |

보스 체력이 라운드가 갈수록 더 가팔라지는 구조(`bossShareBase 2.2 + 0.2/회`)라
세 번째 보스에서 앞 라운드의 **4.1배**가 됐다. `1.9 + 0.12/회` 로 완만하게 폈다.

새 적도 한 구간에 1종씩만 들어오게 나눴다. 재생·분열·보막 셋이 31라운드에
한꺼번에 들어오고 있었다(다른 가지에서 이미 한 번 배운 실수다).

### 해금 간격을 8로 균등화

18/27/36 → **17/25/33**. 확률 밴드 경계도 8/16/24/32 로 함께 옮겼다
(밴드만 어긋나면 "열렸다는데 안 나온다"가 된다 — 검사가 이걸 잡는다).
희귀함이 30라운드 벽보다 먼저 손에 들어온다.

### 결과 (24판)

| hpGrowth | 클리어율 | 사망 분포 |
|---|---|---|
| 1.124 | 85% | 30~34 에 전부 |
| 1.138 | 58% | 30~34 에 6 |
| **1.140 (확정)** | **42%** | 20~24:2 · 25~29:2 · 30~34:4 · 35~39:1 · 40~44:5 |
| 1.143 | 33% | 30~34 에 9 |

도달 라운드 중앙 44, 판 시간 12.1분. **35~39 구간이 처음으로 채워졌다** —
이 프로젝트에서 계속 비어 있던 자리다.

보스 러시는 보스 체력을 편 만큼 되돌렸다(`hpMul 1.0 → 1.3`, 클리어 50%).
챌린지 21% · 엔드리스 중앙 30(최대 58)은 그대로다.

### 검증 도구도 함께 고쳤다

`redesigncheck` 의 조합·창고 검사 4개와 `bootsmoke`·`rendersmoke` 의 검사 4개가
**개체 이름을 하드코딩**하고 있어, 로스터가 바뀐 뒤로 조용히 실패하고 있었다.
전부 `RecipeData` 에서 재료를 가져오도록 고쳤다 — 데이터가 바뀌어도 따라온다.
v1 유물 검사 2개(자유 합성 강조, 각성체 스프라이트)는 v2 에 맞는 검사로 바꿨다.

| 도구 | 전 | 후 |
|---|---|---|
| `redesigncheck` | 98 통과 · 6 실패 | **104 통과 · 0 실패** |
| `rendersmoke` | 오류 3건 | **오류 없음** |
| `bootsmoke` · `integritycheck` · `uicheck` | 이상 없음 | 이상 없음 |

## 고유 스킬 (별도 작업)

리디자인 때 연출만 준비해 두고 비워 뒀던 자리를 채웠다.
그전까지 상위 등급은 "숫자가 큰 포켓몬"이었고, 희귀함과 전설의 차이가 화면에서 안 보였다.

### 무엇이 생겼나

| | |
|---|---|
| 스킬 13종 | 희귀함 8 (쿨다운 12~17초) · 전설 5 (19~23초) |
| 전설 패시브 5종 | 대지의은혜(팀 공격력 +10%) · 융기(광역 반경 +30%) · 심해(때린 적 감속) · 여왕의호령(스킬 쿨다운 -15%) · 집중(보스 피해 +40%) |
| 실행 방식 | `areaBurst` · `single` · `zone`(장판) · `teamBuff` 네 가지. 수치는 `data/skills.js`, 실행은 `SkillManager` |

### 지킨 규칙

- **허공에 쓰지 않는다.** 범위 안에 적이 없으면 쿨다운을 소모하지 않는다.
- **침묵·기절 중에는 쿨다운도 멈춘다.** 보스 패턴과 맞물린다.
- **배율은 실효 공격력 기준.** 강화·시너지·도감·팀 버프가 스킬 피해에도 그대로 실린다.
- **뽑자마자 터지지 않는다.** 첫 발동은 쿨다운 절반을 기다린다.
- **같은 전설을 두 마리 올려도 패시브는 한 번만.** 중복 스택은 후반을 무너뜨린다.
- 전역 스킬도 대상 상한을 둔다. 한 방에 물량을 전부 지우면 라운드가 의미를 잃는다.

### 화면

유닛 둘레에 쿨다운 링이 돌고, 다 차면 금색으로 숨쉰다.
발동하면 스킬 이름이 뜨고, 평타와 확실히 다른 대형 연출이 터진다(리디자인 때 만들어 둔 것).
칸 정보 카드에 스킬 이름·남은 쿨다운·설명과 전설 패시브가 함께 나온다.

### 밸런스 재조정

스킬이 붙자 무전략 봇 클리어율이 **38% → 95%** 로 뛰었다. 체력 곡선을 다시 잡았다.

| hpGrowth | 클리어율 (20~24판) |
|---|---|
| 1.084 (전) | 95% |
| 1.12 | 80% |
| **1.124 (확정)** | **50%** · 도달 중앙 50 · 사망 25~44 에 분산 |
| 1.126 | 38% |
| 1.13 | 35% |

모드 3종은 예전 체력 곡선 기준의 배수를 그대로 쓰고 있어 전부 깨져 있었다. 다시 쟀다.

| 모드 | 전 | 조정 | 후 |
|---|---|---|---|
| 보스 러시 | 중앙 **6**/20 · 0% | `hpMul 2.6 → 1.0` | 중앙 20/20 · **50%** |
| 챌린지 | 중앙 23/50 · 0% | `hpMul 1.12 → 1.0` · `lifeMul 0.5 → 0.75` | 중앙 32/50 · **21%** |
| 엔드리스 | 중앙 **18** | `hpMul 0.9` · 보스 주기 5 → 7 | 중앙 **30** · 최대 59 |

희귀함·전설 해금을 25/34 로 당겨 보기도 했는데, 등급별 확률 밴드가 27/36 에 맞춰져 있어
"희귀함이 25라운드부터 나온다" 검사가 깨졌다. 되돌렸다 — 확률 밴드까지 같이 옮겨야 하는 일이라
이번 범위 밖으로 뒀다.

### 검증

`tools/uicheck.js` 에 스킬 검사 16개를 추가했다(총 44개).
허공 발동 금지, 침묵 중 쿨다운 정지, 강화가 스킬 피해에 실리는지, 팀 버프가 시간이 지나면
되돌아오는지, 장판이 끝나면 사라지는지, 패시브 중복 방지, 판을 다시 시작하면 남지 않는지.

`redesigncheck` 의 체력 배율 허용 범위는 스킬 추가에 맞춰 근거와 함께 넓혔다
(1.05~1.10 → 1.08~1.14, 총 증가 20~70배 → 50~500배).

## UI/UX 리디자인 (게임 HUD 화)

게임 규칙은 한 줄도 바꾸지 않고 화면만 다시 만들었다. 기존 DOM id 와 JS 함수를 유지한 채
레이아웃·CSS 를 갈아엎고, 새 표시는 별도 파일(`ui/Icons.js` · `ui/HudPanels.js`)로 붙였다.

### 무엇을 바꿨나

| 영역 | 전 | 후 |
|---|---|---|
| 필드 | 5:3 캔버스, 넓은 화면에서 좌우 300px 공백 | 보드를 꽉 채우고 가장자리는 숲으로 이어 그린다 |
| 필드 색 | 어두운 풀밭 | 밝은 한낮 풀밭 + 나무·바위·꽃(배경에 한 번만 굽는다) |
| 포켓몬 | 40px, 등급 테두리가 그림을 가림 | 68px, 발밑 그림자 + 칸 모서리 등급 보석 |
| 공격 연출 | 타입 색 선 하나 | 55종 개별 연출(8가지 방식 · 16가지 명중) |
| 조합식 | 이름 텍스트 + ✓/✗ | 재료 그림 + 보유 수 + 진행도, 완성 줄은 금색으로 숨쉰다 |
| 창고 | 개체를 한 줄씩 나열 | 필드+창고를 합친 종류별 그리드, 누르면 상세·배치·조합식 |
| 시너지 | 보유한 타입만 | 10종 전부. 꺼진 것은 흐리게, 누르면 단계표가 펼쳐진다 |
| 확률·도감 | 화면에 없음 | 상단 확률 배지 5종, 우측 도감 진행도와 버프 |
| 칸 정보 | 우측 사이드바 | 고른 칸 옆에 뜨는 카드(클릭은 통과시켜 필드를 막지 않는다) |

### 손맛

버튼에 아래 두께를 주고 누르면 그만큼 내려앉는다. 소환하면 몬스터볼이 흔들리고
결과 카드가 튀어 오르며, 완성 가능한 조합식은 테두리가 숨쉬고, 시너지 단계가 오르면
아이콘이 두 번 맥동한다. 전부 0.1~0.3초다. 동작 줄이기 설정을 켜면 멈춘다.

### 이펙트 성능 (헤드리스 크롬 · 1600×900)

| 상황 | fps | 파티클 |
|---|---|---|
| 실전 부하 (3배속 · 유닛 22 · 적 30 · 45라운드) | **60** | 78 / 1600 |
| 극단 부하 (프레임마다 공격 100건 = 실전의 약 40배) | 33 | 1466 / 1600 (LOD 0.2) |
| 부하 해제 후 | 60 | 14 |

풀이 차면 파티클 수를 스스로 줄이고, 장식용 링부터 건너뛴다.
명중 링처럼 "누가 때렸는지"를 말하는 연출은 줄이지 않는다.

### 에셋 정리

업로드된 55종 PNG 에 액자 테두리와 체크무늬 배경이 구워져 있었다(필드에서 네모로 보이던 원인).
`tools/cleansprites.py` 로 그림은 두고 잔상만 지웠다. 원본은 별도 보관한다.

### 조작 (3차)

- **터치로도 끌 수 있다.** 전에는 탭으로 고르기만 되고 자리를 못 바꿨다(태블릿에서 반쪽이었다).
  마우스와 같은 경로(`pointerDown`/`pointerUp`)를 쓰고, 끄는 동안에는 화면 스크롤을 막는다.
- **시너지 줄을 키보드로 펼친다.** 줄이 `role="button"` 인데 Enter/Space 가 안 먹었다.
- **정보 카드가 보드 밖으로 안 나간다.** 가장자리 칸을 골라도 카드가 잘리지 않게 가둔다.

### 함께 고친 것

- **창고에 자리가 있어도 소환 버튼만 잠기던 버그.** `SummonManager` 는 이미 창고로 보내고 있었는데
  버튼만 필드를 봤다. 시스템 규칙에 맞췄다.
- **검증 도구 3개가 실행조차 안 되고 있었다.** `redesigncheck` 중복 선언,
  `rendersmoke` 의 죽은 파일 목록(`tier.js`·`FusionManager.js`), `bootsmoke` 의 없는 개체 ID.
  이제 `rendersmoke` 는 index.html 의 로드 순서를 그대로 읽는다.

### 검증

| 도구 | 리디자인 전 | 후 |
|---|---|---|
| `redesigncheck` | 통과 98 · 실패 6 | 통과 98 · 실패 6 |
| `bootsmoke` | 이상 없음 | 이상 없음 (화면 패널 검사 7개 추가) |
| `rendersmoke` | 오류 3건 | 오류 3건 |
| `integritycheck` | 문제 없음 | 문제 없음 |
| `uicheck` (신규) | — | 25개 전부 통과 |

남은 실패 6+3 은 리디자인 전부터 있던 것이다(데이터와 테스트가 어긋난 항목).
브라우저에서 드래그 이동·확장 칸 구매·조합·강화·방출·창고 배치·도감·배속·일시정지·
창고 확장·시너지 펼치기를 실제 클릭으로 확인했다.

### 남긴 판단

- **스킬 연출은 준비만 해 뒀다.** 이 빌드에는 스킬 발동 로직이 없다.
  `AttackFx.skill(unit, target)` 과 `unit:skill` 이벤트가 들어오면 그대로 동작한다.
- **UIManager 를 쪼개지 않았다.** 리디자인으로 파일이 더 길어졌지만, 조합식·도감·보유가
  여전히 `refreshActionButtons` 와 공유 배너에 얽혀 있다. 새로 생긴 표시만 `HudPanels.js` 로 뺐다.

조합식 중심 랜덤 디펜스로 재설계 중이다. 설계 근거는 `docs/REDESIGN.md`.

## 완료

| STEP | 내용 | 상태 |
|---|---|---|
| 1 | 등급 ID 를 T1~T5 중립 체계로 전면 교체 | ✅ |
| 2 | pokemon.js 를 계열 구조 → 평평한 개체 목록(55종) | ✅ |
| 3 | `data/recipes.js` + `RecipeManager` | ✅ |
| 4 | FusionManager 제거 → 조각(ShardManager) 로 대체 | ✅ |
| 5 | 5등급 구조 (흔함/안흔함/특별함/희귀함/전설) | ✅ |
| 6 | 라운드별 등급 해금 확률 | ✅ |
| 7 | 고유 스킬 데이터 (실행 로직은 미구현) | 🔸 |
| 8 | UI 재배치 (시너지 우측 / 조합식 하단) | ✅ |
| 9 | 도감 영구 버프 | ✅ |
| 10 | 라운드 대기 제거 · 연속 진행 | ✅ |
| 11 | 템포 조정 (50라운드 10~12분) | ✅ |
| 12 | 밸런스 패스 | ✅ 1차 |

## 확정된 구조

- 개체 55종 (흔함 16 · 안흔함 14 · 특별함 12 · 희귀함 8 · 전설 5)
- 레시피 26개 (안흔함 8 · 특별함 8 · 희귀함 6 · 전설 4)
- 해금 라운드 1 / 11 / 21 / 31 / 41 · 최종 50
- 조합 보정 1.4 (필요 재료 등장 27.8회 → 19.9회, 28% 단축)
- 자동 배치: 그 개체 사거리로 경로를 가장 많이 덮는 빈 칸
- 조각: 방출 시 획득(1/2/4/8/14), 구매(6/14/30/70/150)

## 테스트

```bash
node tools/redesigncheck.js   # v2 필수 검증 59개
```

**기존 `tools/selftest.js` 는 아직 v1 전제로 쓰여 있어 통과하지 않는다.**
자유 합성·계열 구조를 검사하는 약 160개가 폐기 대상이다. STEP 8 이후 정리한다.
그때까지 v2 의 안전망은 `redesigncheck.js` 다.

## 아직 안 된 것 (중요)

- 고유 스킬은 데이터만 있고 발동 로직이 없다 (STEP 7 마무리)
- **밸런스 미조정** — 실제 전투로 50라운드를 버틸 수 있는지 아직 안 쟀다 (STEP 12)
- `tools/selftest.js` 392개 중 v1 전제 약 160개가 아직 정리 안 됨

## 라운드 진행 (STEP 10~11 완료)

v1 은 라운드마다 8초 정지였고 그게 전체 시간의 16% 였다.
v2 는 멈추지 않는다. 소환·배치·조합·강화를 전투 중에 한다.

```
SPAWNING ──스폰 끝──▶ CLEARING ──▶ INTERLUDE(0.8초) ──▶ 다음 라운드
```

CLEARING 을 무한정 기다리지는 않는다. 적을 다 잡으면 0.8초 뒤 바로 다음 라운드가 오지만,
못 잡고 있으면 6초 뒤에 다음 라운드가 **겹쳐서** 들어온다.
강하면 빠르게, 밀리면 더 몰린다.

| 지표 | v1 | v2 |
|---|---|---|
| 최종 라운드 | 20 | **50** |
| 라운드 사이 정지 | 8초 | **0.8초** |
| 라운드당 시간 | 36초 | **12.3초** |
| 한 판 | 12분 (20라운드) | **10.2분 (50라운드)** |
| 체력 배율 | 1.202/라운드 | 1.112/라운드 (50라운드 기준 170배) |

난이도는 물량이 아니라 체력·방어력·보스 패턴으로 올린다 (기획 §23).
라운드당 스폰 시간 11초가 상한이고, 물량은 9→31마리로만 는다.

## 플레이 피드백 반영 (4차) — 창고 · 조합식 개편

| 요청 | 처리 |
|---|---|
| 시너지·선택창 스크롤 | 패널마다 개별 스크롤 + 얇은 스크롤바 |
| 골드 사용처 | 확장 칸 **2 → 6개** (200~1800G) + 창고 확장 (120G부터) |
| 창고 | **StorageManager 신설** |
| 초반 난이도 | 1~20라운드 체력 가중치(R1 ×1.45 → R20 ×1.0) + 물량 상향 |
| 조합식 | 재료 수를 등급별로 차등, 상위 등급 성능 상향 |

### 창고

조합식이 29개인데 다 외울 수 없다. 그 상태에서 필드가 차면
"뭘 버릴까"가 고민이 아니라 도박이 된다. 잘못 버리면 조합이 막힌다.

- 기본 14칸, 골드로 40칸까지 확장
- 소환·조합 결과가 필드에 자리 없으면 창고로 간다
- **조합은 필드와 창고를 합쳐서 재료를 찾는다** (창고를 먼저 쓴다 — 필드 것은 싸우는 중이므로)
- 창고 칸을 누르면 필드에 배치. 필드가 차 있으면 자리를 맞바꾼다
- 조합에 곧 쓰일 개체는 **초록 테두리 + "재료"** 표시로 앞에 정렬된다

고민이 "버릴까 말까"에서 **"어떤 걸 올릴까"** 로 바뀐다.

### 조합식 구조 변경

```
안흔함 = 흔함 ×2
특별함 = 안흔함 ×2 + 흔함 ×1
희귀함 = 특별함 ×2 + 안흔함 ×1
전설   = 희귀함 ×2 + 특별함 ×1
```

누적 비용(흔함 환산): 2 / 4 / 8 / 19마리. 어려워진 만큼 성능도 올렸다.

| 등급 | 전 | 후 |
|---|---|---|
| 특별함 | 180~320 | **220~400** (×1.25) |
| 희귀함 | 600~1100 | **870~1600** (×1.45) |
| 전설 | 2000~3500 | **3400~5600** (×1.7) |

### 난이도 재조정 기록

창고가 생기자 재료를 다 모아 둘 수 있어 클리어율이 **90%** 로 뛰었다
(전설이 보드 18칸 중 10.4마리). 체력 배율을 1.068 → 1.084 로 올려 **50%** 에 맞췄다.

| 배율 | 클리어율 | 도달 중앙 |
|---|---|---|
| 1.068 | 90% | 50 |
| 1.079 | 80% | 50 |
| **1.084** | **50%** | **50** |
| 1.090 | 30% | 34 |

## 밸런스 패스 1차 (STEP 12)

자동 플레이 도구(`tools/autoplay.js`)를 v2 용으로 새로 쓰고 10~12판씩 돌려 가며 잡았다.
각 수치는 측정 → 조정 → 재측정으로 근거를 남겼다.

### 무엇이 문제였나

| 측정 | 문제 | 조정 |
|---|---|---|
| 10판 전부 33~41라운드에서 사망 | 체력 곡선이 플레이어 성장보다 가팔랐다. 필요 실효 DPS 7,600 인데 보드는 3,700 | hpGrowth 1.112 → **1.068** |
| 마기라스가 10판 중 9판 피해 1위 | 광역이 물량전에서 표시 DPS 보다 훨씬 세다 | 광역 3종 하향, 단일 2종 상향 |
| 조각 구매 판당 43.7회 | "재료가 안 나온다"는 긴장이 사라진다. 조각은 구제책이지 상점이 아니다 | 획득 하향 · 가격 2배 |
| 도달 중앙 42인데 전설 해금 41 | **전설이 존재하지 않는 콘텐츠였다** | 해금 11/21/31/41 → **1/9/18/27/36** |
| 42라운드에서 8/10 사망 | 마지막 구간에서 스폰 간격이 0.45→0.40 으로 급가속. 잔당과 겹쳐 눈덩이 | 간격 유지 · 물량 상한 |
| 클리어 판 14.2분 | 목표 10~12분 초과 | interlude 0.8→0.6, carryOver 6→5.5, 후반 물량 완화 |

### 결과 (노멀 12판)

| 지표 | 값 |
|---|---|
| 클리어율 | **50%** (6/12) |
| 도달 라운드 | 최소 42 · 중앙 **50** · 최대 50 |
| 판 시간 | **12.6분** |
| 판당 소환 / 조합 / 방출 | 205 / 117 / 129회 |
| 최종 보드 | 희귀함 8.9 · 특별함 3.7 · 전설 0.7 |

### 남은 문제 (솔직하게)

- **사망이 40~44에 100% 몰린다.** 마지막 관문이라 자연스러운 면도 있지만 국지적이다
- **보스 러시 75%, 챌린지 0%** — 모드 편차가 크다. 챌린지는 32~33에서 멈춘다
- 표본이 4~12판이라 **노이즈와 신호를 가르기 어렵다.** 모드별 조정은 30판 이상 놓고 다시 봐야 한다
- 강화가 판당 5~10회로 쓰이긴 하나 여전히 소환·조각에 밀린다

`redesigncheck.js` 에 밸런스 회귀 테스트 9개를 넣어 두었다.
체력 배율 범위, 라운드당 스폰 예산 13초, 물량 대 체력 증가 비율,
해금 간격 균등성 등을 검사한다. 나중에 수치를 만지면 여기서 걸린다.

## 플레이 피드백 반영 (3차)

| 문제 | 원인 | 수정 |
|---|---|---|
| 조합식 펼치면 커서와 클릭 위치가 어긋남 | 조합식이 커지며 캔버스가 줄어드는데 `resize` 는 **창 크기 변화에만** 반응했다. `scale` 이 옛 값으로 남았다 | ResizeObserver 로 캔버스 자체를 관찰 + 좌표 변환 시 rect 에서 직접 계산 |
| 골드가 너무 쉬움 | 라운드당 **5.9회** 소환할 골드가 들어왔다. 슬롯은 18칸인데 50라운드 동안 **296회** 뽑을 수 있었다 | 수입 1/3 로, 비용 상한 130→190. 라운드당 **2.4회** |

### "소환을 없애고 라운드마다 지급" 제안에 대한 판단

**소환은 유지하고 무료 지급을 더했다.** 소환을 없애자는 제안은 받지 않았다.

리디자인 지시서 §1 의 핵심 루프 첫 줄이 "현재 라운드의 소환 확률에 따라 랜덤 소환"이고,
§8 라운드별 확률·§9 등급 해금 연출·§31 랜덤성 설계가 전부 그 위에 얹혀 있다.
§39 판정 기준 1번 "랜덤 소환에 기대감이 있는가" 도 즉시 NO 가 된다.

골드가 남은 건 소환이 있어서가 아니라 **수입 곡선이 잘못돼서**였다.
시스템을 없애는 건 과잉 대응이라고 봤다.

대신 **라운드마다 흔함 1마리를 무료로 준다.**
- 1~10라운드에 소환 말고 할 일이 없던 공백을 메운다
- "라운드마다 뭔가 받는다"는 리듬을 만든다
- 조합 보정이 그대로 걸려서 필요한 재료가 공짜로 올 수도 있다
- 소환은 "언제 지를까"의 선택으로 남는다

| 지표 | 전 | 후 |
|---|---|---|
| 라운드당 소환 가능 | 5.9회 | **2.4회** |
| 50라운드 총 소환 | 296회 | **121회** |
| 무료 지급 | 없음 | 50마리 |
| 총 유입 | 296마리 | **171마리** |

## 플레이 피드백 반영 (2차)

| 문제 | 원인 | 수정 |
|---|---|---|
| 칸 이동이 엉뚱한 곳으로 감 | **칸끼리 세로 28px 겹침.** 좌표 탐색 때 경로 이격만 보고 칸끼리는 안 봤다 | 칸 크기 68→56, 좌표 재탐색. 겹침 0, 최소 간격 60px |
| 1~10라운드에 할 일이 없음 | 조합식을 결과물 등급 따라 11/21/31/41 로 잠갔다 | **전부 1라운드부터 개방** |
| "전체" 눌러도 4줄만 보임 | 필터와 무관하게 `slice(0, 4)` | 전체는 영역이 커지고 2열로 스크롤 |

조합식을 1라운드에 다 열어도 무너지지 않는다. 상위 재료는 소환에서 열리기 전까지
조합으로만 얻어야 하고 골드가 그 속도를 제한한다. 오히려 "소환에 없는 등급을
조합으로 먼저 만든다"는 선택지가 생긴다.

**조합식 수정 방법은 `docs/RECIPES_GUIDE.md` 참고.**

## 레이아웃 수정 (피드백 반영)

플레이해 보니 조합식이 필드에 가려지고 선택한 칸 정보가 잘렸다. 원인이 셋이었다.

1. **그리드 행을 3개로 두고 영역을 4개 넣었다.** 조합식이 암묵적 행으로 생겨
   필드가 `1fr` 을 다 먹고 아래로 밀려났다. → `auto / 1fr / auto / auto` 로 수정
2. **캔버스가 폭 기준으로 높이를 정했다.** `width:100% + aspect-ratio` 라
   세로 공간을 전부 차지했다. → 높이 기준으로 잡고 폭이 남으면 가운데 정렬
3. **`coverageHtml()` 호출만 있고 정의가 없었다.** 빈 칸을 클릭하면 그 자리에서
   터졌다. 사이드바가 깨져 보인 직접 원인이다.

정보량도 줄였다. 선택한 칸 카드가 410px 였는데 사이드바 가용 높이가 270px 였다.

| | 전 | 후 |
|---|---|---|
| 정보 행 | 10줄 | **6줄** |
| 칸 설명 | 사거리 3종 커버리지 px 막대 | **한마디** (근접용 / 중거리용 / 장거리 전용 / 구석 자리) |
| 카드 높이 | 약 410px | 약 292px |

기획 지침 "보고 바로 이 자리는 장거리용 정도를 이해할 수 있어야 한다" 를 따랐다.
16칸 성격 분포는 근접용 9 · 중거리용 3 · 장거리 전용 2 · 구석 자리 2 다.

해상도 1280×720 ~ 1920×1080 에서 전부 한 화면에 들어가는 것을 계산으로 확인했다.
1100px 미만이거나 세로 700px 미만이면 스크롤을 연다.

## 화면 구조 (STEP 8 완료)

```
상단   라운드 · 다음 해금 예고 · 골드 · 라이프 · 배속 · [도감]
중앙   전투 필드              │ 시너지 (우측 고정)
                             │ 선택한 칸
하단   조합식 (필드 바로 아래 고정 · 최대 4줄 · 완성순 정렬)
바닥   소환 / 조합 / 강화 / 방출 · 조각 수
```

조합식 줄은 `파이리 ✓ + 리자드 ✗ → 특별함 ???` 형태다.
완성 가능한 줄은 테두리가 숨쉬고, 아직 안 만든 결과물은 `???` 로 가린다.
