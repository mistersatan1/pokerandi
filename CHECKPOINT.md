# UI/UX 리디자인 체크포인트

작업을 이어서 할 때 이 문서를 먼저 읽는다. (기존 `HANDOFF.md` 는 게임 시스템 인수인계,
이 문서는 **리디자인 진행 상황**만 다룬다.)

---

## ★ 세션 32 — 조합식 개편 v2 적용 (가장 최근)

아래 기록 중 **계열 추첨(PoolManager) · `any:` 와일드카드 · 히든 등급(TH) · `_immortal` 폼**에 대한 내용은
전부 옛 이야기다. 지금 구조와 측정값은 `VERSION.md` 맨 위 "조합식 개편 v2" 를 본다.
다음 할 일: ① v2 밸런스 조정(클리어 25% → 0%) ② 메타몽 소환/히든 결정 ③ 시라소몬 비용 이상 ④ 남은 병목 17곳.

## 0. 작업 기준이 된 사실 (다시 조사하지 말 것)

- 이 zip 은 `HANDOFF.md` 가 설명하는 빌드와 **다른 가지**다.
  SkillManager · TutorialManager · `data/skills.js` · `data/tutorial.js` · `btnStore` · 안내 띠가 **없다**.
  로스터는 1세대 55종 PNG 로 교체돼 있고 조합식은 **39개**다.
- **스킬 발동 로직이 없다.** 그래서 스킬 연출은 데이터·렌더러까지만 준비돼 있다
  (`AttackFx.skill(unit, target)` 또는 `unit:skill` 이벤트가 오면 즉시 동작).
- 요청서 예시의 고오스·스라크·마기라스·뮤츠·레쿠쟈는 이 로스터에 **없다**.
  얼음·에스퍼·고스트·바위·강철·드래곤 타입 개체도 없다(스타일 정의는 미리 넣어 뒀다).
- 원본 데이터 문제 2건은 **손대지 않았다** (리디자인 범위 밖).
  ① 결과보다 재료 등급이 높은 조합식 6개 ② 도달 불가능한 시너지 5종.

## 1. 검증 기준선 (리디자인 전 = 후, 회귀 없음)

| 도구 | 원본 | 현재 |
|---|---|---|
| `node tools/redesigncheck.js` | 통과 98 · 실패 6 | **통과 98 · 실패 6** |
| `node tools/bootsmoke.js` | 이상 없음 | **이상 없음** |
| `node tools/rendersmoke.js` | 오류 3건 | **오류 3건** |
| `node tools/integritycheck.js` | 문제 없음 | **문제 없음** |

남은 실패 6 + 3 은 전부 **원본에서 이미 실패하던 것**이다(데이터와 테스트가 어긋난 항목).
도구 3개는 원본 상태에서 아예 실행되지 않아 먼저 고쳤다 —
`redesigncheck` 중복 선언, `rendersmoke` 의 죽은 파일 목록(`tier.js`·`FusionManager.js`),
`bootsmoke` 의 없는 개체 ID. **이제 rendersmoke 는 index.html 의 로드 순서를 그대로 읽는다.**

## 2. 완료 (STEP 1~9 대부분)

### 새로 만든 파일
| 파일 | 내용 |
|---|---|
| `js/data/attackfx.js` | 55종 개별 공격 연출 설정 + 타입 18종 기본 스타일 |
| `js/render/AttackFxRenderer.js` | 투사체·빔·브레스·참격·폭발·번개·광역·궤도 8종 + 명중 16종. 파티클 풀 1600(Float32Array), LOD 자동 감쇠 |
| `js/ui/Icons.js` | 타입 SVG 아이콘 17종, 포켓몬 이미지 헬퍼(`RPD.UI.sprite`), 실패 시 SpriteFactory 대체 |
| `js/ui/HudPanels.js` | 상단 확률 배지 · 사거리 칩 · 도감 요약 · 소환/조합 토스트 · 버튼 반동 · 라이프 피격 |
| `tools/cleansprites.py` | PNG 에 구워져 있던 액자 잔상·체크무늬 제거 스크립트 |
| `CHECKPOINT.md` | 이 문서 |

### 고친 파일
| 파일 | 변경 |
|---|---|
| `index.html` | 구조 전면 교체(HUD / 필드 / 우측 시너지·도감 / 하단 소환·조합식·보유). **기존 DOM id 전부 유지**, 새 id 12개 추가, 새 스크립트 4개 등록 |
| `css/main.css` | 토큰(네이비+골드) · 그리드 레이아웃 · 반응형 |
| `css/game.css` | 보드·상태 알약·배너·연출 카드·칸 정보 카드·시작 패널·오버레이 3종 |
| `css/ui.css` | 패널/버튼 공통 규칙, HUD, 소환·강화·방출, 조합식 행, 보유 그리드, 시너지, 도감 요약 |
| `js/ui/UIManager.js` | 칸 정보 카드(필드 위 부유) · 조합식 새 마크업 + 필터 3종 + 서명 캐시 · 시너지 10종 전체 + 펼치기 · 보유 포켓몬 종류별 그리드 + 상세 창 · 상태 클래스(`body[data-state]`) |
| `js/render/Renderer.js` | 캔버스 풀블리드(contain 맞춤 + 오프셋) · `toCanvasCss` · `logicalBounds` · 화면 흔들림 훅 |
| `js/render/MapRenderer.js` | 밝은 풀밭 팔레트 · 나무/바위/꽃/연못 장식(배경에 한 번만 구움) · 화면 끝까지 이어지는 길 |
| `js/render/UnitRenderer.js` | 스프라이트 40→68px · 발밑 그림자 · 등급 보석 · 선택 펄스 · 공격 연출은 AttackFx 에 위임 |
| `js/render/FxRenderer.js` | 섬광이 확장 영역까지 덮도록 |
| `js/main.js` | AttackFx · HudPanels 초기화/업데이트/리셋/레이어 등록 |
| `assets/pokemon/*.png` | 55종 액자 잔상 제거 (원본은 업로드한 zip 에 있다) |

### 기능 확인
`bootsmoke` 로 소환·배치·선택·방출·조합·창고 배치·도감·배속·일시정지·보스까지 실제 클릭 경로 통과.
1920×1080 / 1366×768 / 1280×720 에서 스크롤 없이 한 화면(에러 0).

### 알려진 UI 버그 1건 수정
필드가 가득 차면 창고에 자리가 있어도 **소환 버튼만 잠기던 것**을 시스템 규칙에 맞췄다
(`SummonManager` 는 원래 창고로 보내고 있었다).

## 2-b. 2차 세션에서 추가로 끝낸 것

- **이펙트 성능 측정 완료** — 실전 부하 60fps, 극단 부하(실전의 40배) 33fps, 부하 해제 후 즉시 복귀.
  링 풀이 마르면 장식 링을 건너뛰는 LOD 를 추가했다. 수치는 `VERSION.md` 에 표로 남겼다.
- **`tools/uicheck.js` 신규 (25개)** — 개체별 연출 설정·중복 없음·풀 상한·자동 정리·스킬 API·
  화면 요소·반응형 분기·동작 줄이기 설정.
- **`tools/bootsmoke.js` 에 화면 패널 검사 7개 추가** — 확률 배지, 사거리 칩, 시너지 10종,
  조합식 그림·진행도, 보유 그리드, 칸 정보 카드 열고 닫기, 창고 여유 시 소환 버튼.
- **브라우저 실조작 확인** — 드래그 이동, 확장 칸 구매, 시너지 펼치기, 조합식 탭 3종,
  보유 상세창→배치, 도감, 배속, 일시정지, 강화, 방출, 창고 확장. 전부 정상.
- **칸 정보 카드가 필드 클릭을 막던 문제 수정** — 카드는 클릭을 통과시키고 닫기 버튼만 받는다.
  (확장 칸 구매 테스트에서 발견)
- **1100px 미만 레이아웃 정리** — 패널 높이를 고정하고 각자 스크롤. 1024×768 · 820×1180 확인.
- **문서 갱신** — `README.md` 화면·그래픽·도구 절, `VERSION.md` 리디자인 절.

## 2-c. 3차 세션에서 끝낸 것

- **터치 조작** — `touchstart/move/end/cancel` 을 마우스와 같은 경로로 통합.
  탭 선택 · 터치 드래그 이동 · 탭으로 확장 칸 구매를 터치 에뮬레이션으로 확인.
- **키보드** — 시너지 줄 Enter/Space.
- **정보 카드 위치 보정** — 보드 경계 안으로 가둔다.
- **회귀 테스트 3개 추가** (`uicheck` 총 28개) — 터치 경로 · 키보드 · 카드가 클릭을 막지 않음.
- 스프라이트 잔여 점: 조사 결과 **떨어져 있는 조각이 아니라 그림에 붙어 있는 픽셀**이라 두었다.
  지우면 원화가 깎인다.

## 2-d. 4차 세션 — 고유 스킬 (별도 작업)

리디자인 범위 밖으로 미뤄 뒀던 스킬 시스템을 따로 구현했다.

- **새 파일**: `js/data/skills.js`(스킬 13 + 패시브 5), `js/systems/SkillManager.js`.
- **연결**: `UnitManager`(스킬 부착·버프 반영), `CombatManager`(패시브 3종), `main.js`,
  `index.html`, `tools/autoplay.js`, `tools/integritycheck.js` 의존성 표.
- **화면**: 쿨다운 링, 스킬 이름 표시, 칸 정보 카드의 스킬·패시브 줄.
  연출은 리디자인 때 만들어 둔 `AttackFx.skill` 이 그대로 살아났다.
- **밸런스**: `hpGrowth 1.084 → 1.124`, 모드 3종 재조정. 수치와 근거는 `VERSION.md`.
- **검사**: `uicheck` 에 스킬 16개 추가(총 44개). 나머지 도구는 기준선 유지.

## 2-e. 5차 세션 — 데이터 정리와 난이도 재분배

- **등급 역전 조합식 6개 수정** · **켜질 수 없는 시너지 3종 제거**(드래곤·에스퍼·고스트).
- **30~34 사망 몰림 해소**: 원인은 30라운드 보스였다(앞 라운드의 4.1배).
  보스 체력 증가를 `2.2+0.2/회 → 1.9+0.12/회` 로 펴고, 특수 적을 한 구간에 1종씩 나눴다.
- **해금 간격 8 균등화** (17/25/33) + 확률 밴드 동반 이동 (8/16/24/32).
- **hpGrowth 1.124 → 1.140** (24판 42% · 중앙 44 · 사망이 20~44 에 고르게 분포).
  보스 러시 `hpMul 1.0 → 1.3`.
- **하드코딩 때문에 죽어 있던 검사 8개 복구** → 도구 5종 전부 통과
  (`redesigncheck` 104/0, `rendersmoke` 0건).

## 2-f. 6차 세션 — 조각 상점

`HANDOFF.md` 7번의 "바로 해야 할 일"이었는데 리디자인 범위로만 읽고 놓치고 있었다.

- 조합식 패널에 `[◆ 조각 상점]` 탭 추가. 조합에 바로 필요한 재료가 맨 위.
- `ShardManager.buy()` 가 필드 빈 칸만 보던 것을 **필드 → 창고** 로 (소환과 같은 규칙).
- 가격 70% 인상(판당 구매 10.5 → 5.4회) · `hpGrowth 1.140 → 1.142` (24판 50%).
- `uicheck` +7(총 51) · `bootsmoke` +1. 도구 5종 전부 통과 유지.

## 2-g. 7차 세션 — 플레이 피드백 6건

보유 정렬(필드 먼저) · [창고로] 버튼 · 조합식 등급 정렬과 등급 칩 · 창고 개체 방출 ·
등급 테두리 통일 · 보유 상세창 재료 표시와 조합식 연동. 자세한 내용은 `VERSION.md`.

**주의**: 자동 플레이 클리어율이 같은 설정에서 19~50% 로 흔들린다(16~24판 표본).
밸런스 수치를 다시 만질 때는 반드시 40판 이상으로 재고, 20판 결과를 신호로 읽지 말 것.

## 2-h. 8차 세션 — 밸런스 재측정 (30~40판)

노멀 40판 43% · 보스 러시 30판 57%(`hpMul 1.3 → 1.22` 조정) · 챌린지 30판 13% ·
엔드리스 30판 중앙 30. 이 값이 현재 기준선이다. 표와 근거는 `VERSION.md`.

## 2-i. 9차 세션 — 테두리 규칙 · 보유 정렬

빨간 테두리 제거(등급 전용) · 보유 칸 테두리 통일(재료는 점, 창고는 배지) ·
보유 정렬 4종(필드/등급/수량/이름). 중복으로 들어가 있던 정렬 코드 한 벌 제거.
`uicheck` +2(총 57).

## 2-j. 10차 세션 — 규칙 변경 5건

출구 칸 · 조합식 ??? 제거 · 라이프 20→60 · 시너지 4종 추가(독·노말·벌레·페어리) ·
소환 상한 특별함. 난이도는 **수입 -25%** 로 다시 잡았다(체력·방어·물량은 듣지 않았다.
물량은 오히려 쉬워졌다 — 처치 골드가 늘기 때문).

**노멀 30판 43%** (사망이 25~49 전 구간에 퍼진다).
모드 3종도 30판으로 재측정: 보스 러시 57%(`hpMul 1.22 → 1.4`) · 챌린지 17% · 엔드리스 중앙 51.

## 2-k. 12차 세션 — 소리

`js/core/AudioManager.js` 신규. 효과음 19종 · 배경음 3곡, 전부 WebAudio 합성(음원 파일 없음).
상단 스피커 버튼에 음량·음소거(저장됨). 쿨다운·동시 발음 상한·탭 전환 정지.
`uicheck` +8(총 65).

## 2-l. 13차 세션 — 적 몬스터 디자인

디자인 시트에서 49장 추출 → `assets/enemies/digimon/`. `js/data/enemyskins.js` 가
구간 × 성질 → 그림·이름을 정한다(전투 수치 불변). 보스 체력바 위치 조정. `uicheck` +5(총 78).
쓰지 않은 그림 4장(정예 군체 3 · 메탈그레이몬 군체)은 파일만 있다.

## 2-m. 14차 세션 — 소환 누출 · 조합식 창 · 보스 보상

- 천장·소환권으로 희귀함·전설이 새던 것(약 2.5%) 수정. 쓰이지 않던 소환권이 이제 쓰인다.
- 조합식 그림 클릭 → 그 포켓몬의 조합식 창(재료로 내려가기·뒤로·재료로 쓰이는 곳).
- `js/systems/RewardManager.js` 신규 — 보스 보상(10·20·30·40·50R, 이후 10R마다).
- 노멀 30판 40%(변화 없음) · 보스 러시 `hpMul 1.48` 로 57%. `uicheck` +9(총 87).
- **챌린지·엔드리스는 이번 변경 뒤 다시 재지 않았다.**

## 2-n. 15차 세션 — 안흔함 조합식 = 같은 흔함 2마리

안흔함 14종을 진화 전 모습 ×2 로. UI 는 `×2` 배지로 묶어 표시. 재료 칸별 보유 판정 수정.
검사: "같은 재료로 서로 다른 결과 금지" 추가, 하드코딩 검사 4개를 데이터 기반으로.
노멀 73% → `killGoldPerWaves 12` 로 50%. `docs/RECIPES_GUIDE.md` 새로 씀.
**보스 러시·챌린지·엔드리스는 재지 않았다.**

## 2-o. 16차 세션 — 테스터용 단일 파일

`node tools/build-tester.js` → `dist/포켓몬랜덤디펜스_테스트.html`(약 2.3MB, 전부 인라인).
`Assets.url()` 로 그림 경로를 인라인 데이터로 돌린다. 적 스킨 로딩 중 404 제거.
**게임을 고친 뒤 테스터에게 다시 보낼 때는 빌드를 다시 돌려야 한다.**

## 2-p. 18차 세션 — 특성

`js/data/traits.js`(데이터·수정 안내) + `js/systems/TraitManager.js`. 피카츄·고라파덕·나옹·파라스.
공격 시 확률은 공격 1회당 1번. 노멀 30판 60%(노이즈 범위, 조정 안 함). `uicheck` +8(총 104).

## 2-q. 19차 세션 — 특성 22종

특별함 12 전부 · 안흔함 6 · 흔함 4. 종류 13가지(TraitManager). 보스는 기절·밀치기 면역.
`CombatManager.fireOnce`(삼연타용) · `TraitManager.applyStats`(UnitManager.recompute 에서 호출).
노멀 70% → `hpGrowth 1.152` 등으로 40%. **다른 모드 3종은 재지 않았다.** `uicheck` 총 98.

## 2-r. 20차 세션 — 희귀함 특성 5종

라이츄·독침붕·피죤투·고지·픽시. 새 종류 armorBreakOnAttack, statBonus.critRateAdd.
직전 "40%" 가 운이었음을 확인(재측정 23%·27%) → `hpGrowth 1.146` 으로 60판 40%.
**수치 확정은 30판 × 2회로.** `uicheck` 총 101.

## 2-s. 21차 세션 — 단축키·끌기·타입별·흔함50%·조각가격·버퍼패시브

단축키(HudPanels.bindHotkeys) · 창고↔필드 끌기(UIManager ownedDrag*/overOwnedPane) ·
보유 타입별(renderByType) · 흔함 최소 50%(SummonTable.clampCommon) · 조각 5/10/25 ·
버퍼 패시브(`js/data/auras.js`, UnitManager.auraExtrasFor). 노멀 60판 50%.
**다른 모드 3종은 재지 않았다.** `uicheck` 총 109.

## 2-t. 22~23차 세션 — 설명서 · 난이도 · 칭호

게임 안 설명서(? / H) + `docs/설명서.md`, 설명서 숫자 동기화 검사.
노멀 → 일반, 난이도 4단계(`RPD.Difficulties`, `RPD.effectiveMode`), 기록 키 `NORMAL:<난이도>`.
칭호·시작 보너스(`js/data/titles.js`, `js/systems/ProgressManager.js`).
실측: 쉬움 87 · 보통 50 · 어려움 27 · 지옥 0%. `uicheck` 총 119.
(24차에 재측정 완료 — 아래 2-u)

## 2-u. 24차 세션 — 모드 3종 재측정

보스 러시 90% → `hpMul 2.1` 로 55%(60판). 챌린지 20% · 엔드리스 중앙 49 는 그대로.
**모든 모드의 기준선이 최신이다.** 표는 `VERSION.md` 맨 위.

## 2-v. 25차 세션 — 1세대 그림 95장

`assets/pokemon/` 에 95장 추가(로스터 미반영). 목록 `assets/pokemon/NEW_SPRITES.md`.
테스트판은 로스터에 있는 그림만 넣는다. **다음: 이 95종을 pokemon.js 에 넣고 조합식·특성·공격 연출·적 배치 정하기.**
(26차) 잘림·갇힌 체크무늬 수정, 골덕·강챙이 추가 → 1세대 151종 그림 완비(+왕구리).

## 2-w. 27~28차 세션 — 1세대 150종 1단계

로스터 150종(pokemon.js, evolvesFrom) · 기존 10종 등급 정리 · 조합식 91개(any:타입:등급 도우미) ·
계열 추첨(`js/systems/PoolManager.js`) · 메타몽 대체(RecipeManager) · 시너지 17종 · 스킬 33 · 전설 패시브 10 ·
특성 61 · 버퍼 5 · 연출 150. 노멀 60판 48%(`hpGrowth 1.109`). 생성 스크립트 `tools/gen1/`.

(2단계 완료 — 아래 2-y)
설계 합의: 히든(왕구리·뮤, 채팅, 무제한) · 불멸(전설 3+, 채팅, 무제한, 전설 < 불멸 < 초월) ·
초월(전설 1+ + 40R 보스 보상 '초월의 조각', 채팅, 판당 1회). 주문·대사는 사용자가 적을 수 있는 파일로.
**그 뒤: 다른 모드·난이도 재측정.**

## 2-x. 29차 세션 — 조합 사전

`js/ui/RecipeBook.js` — 원랜디 조합식 사이트 참고. 등급 칩 · 흔함 환산 · 초성 검색 · 보유 기준 정렬 · R 단축키.
**히든·불멸·초월(2단계)이 들어오면 사전에도 등급 칩을 추가하고, 발견 전에는 ??? 로 가린다.**

## 2-y. 30~31차 세션 — 히든 · 불멸 · 초월 + 채팅 주문

`js/data/spells.js`(주문 7개 예시) · `js/systems/SpellManager.js` · `js/ui/SpellUI.js`.
등급 TH/T6/T7(`ALL_TIERS`, `tierPower`). 40R 보상에 초월의 조각. 조합 사전에 발견한 주문 공개.
연출: 대사 타자기 · 전용 곡 3 · 필드 오라. `uicheck` 총 139.
**다음: 다른 모드·난이도 재측정(150종 이후 한 번도 안 쟀다).**

## 3. 남은 작업 (다음 세션은 여기부터)

리디자인 자체는 끝났다. 남은 것은 **사람이 직접 해 봐야 아는 것**과 선택 항목이다.

1. **실제 플레이 피드백** — 한 판 돌려 보고 정보 위치가 맞는지.
   특히 ① 칸 정보 카드가 필드 어디에 떠야 편한지 ② 조합식 줄 높이가 적당한지
   ③ 상단 확률 배지를 실제로 보는지.
2. **스킬 체감 확인** — 봇 기준으로는 맞췄지만, 사람이 보기에 스킬이 "터졌다"고 느껴지는지,
   쿨다운 13~23초가 지루하지 않은지는 직접 해 봐야 안다.
3. 선택: 드래곤·에스퍼·고스트 타입 포켓몬을 로스터에 넣고 시너지 3종을 되살리는 일.
4. 선택: 챌린지 모드 도달 중앙이 23 으로 낮다. 노멀만큼 손보지 않았다.

## 4. 다음 대화 시작 시

```bash
node tools/integritycheck.js && node tools/bootsmoke.js && \
node tools/uicheck.js && node tools/rendersmoke.js && node tools/redesigncheck.js
```
기대값: 문제 없음 · 이상 없음 · 이상 없음 · 오류 3건 · 통과 98 실패 6.
